# PifPaf Pulse — спецификация

**Внутренний сервис аналитики рилсов для блогеров PifPaf AI.**

> Этот файл — **спецификация**: что строим, зачем и в каких рамках.
> Пошаговый план исполнения живёт отдельно в `docs/superpowers/plans/`.
> Claude Code читает оба файла и держит по ним контекст между сессиями.

**Название в интерфейсе:** «PifPaf Pulse». В шапке и в письме формулировать как
«PifPaf Pulse — внутренний сервис аналитики для блогеров PifPaf AI»: сразу понятно,
что это инструмент для них, и нет вида, будто бренд присвоен.

---

## Режим работы

| Ограничение | Значение |
|---|---|
| Срок | **Дедлайна нет.** Работаем вертикальными срезами, качество важнее скорости. |
| Бюджет | **0 ₽.** Только бесплатные тарифы. Домен не покупаем — живём на `*.vercel.app`. |
| Аккаунты | Apify, Neon, Vercel, GitHub — уже есть. |

Отсутствие дедлайна меняет приоритеты: пишем тесты на нормализацию данных и SQL,
не режем полировку, доводим каждый вертикальный срез до конца перед началом следующего.
Отсутствие бюджета — не косметическое ограничение, оно **диктует архитектуру
синхронизации** (см. §1 и §7).

---

## 0. Техническое задание и его разбор

### Оригинал, дословно

> Сделать сайт для внутренних блоггеров, где будет обновляться по ссылке из инсты
> просмотры, дата, обложка и все органично подтягиваться. Данные подтягиваются из
> апифая по апи (бесплатки хватит)
>
> Там же будет список, как бд красивая или в форме ленты этих видео.
>
> Дизайн возьми с пинтереста, цвета и вайб, в стиле нашего приложения - pifpafai.com
>
> На сайте как для наших блоггеров личные кабинеты типо будет все считаться, то есть
> разные аккаунты с разными рилсами - они туда грузят и все удобно видно.
>
> Красивый формат дашборда. Аналитики и личного кабинета.
>
> джаваскрипт, скл. дедлайн - 24 часа. Важно! задеплоить сайт и показывать сайт а не
> гит. Смотртим на детали и на очеловеченность.
>
> тестовое отправлять в тг - @sergeyzolotykh

### Разбор: что требуется буквально

| Цитата | Что за этим стоит | Где закрыто |
|---|---|---|
| «обновляться по ссылке из инсты **просмотры, дата, обложка**» | Названы **ровно три поля**. Лайки и комментарии — бонус, эти три — обязательны и должны работать безупречно. | §6, §8 |
| «**обновляться**» | Не разовый парсинг: повторная синхронизация. | §7 |
| «всё **органично** подтягиваться» | Вставил ссылку → всё само. Ноль ручного ввода полей. | §5, §10 |
| «из апифая по апи **(бесплатки хватит)**» | **Это условие задачи, а не ремарка.** См. врезку ниже. | §1, §7 |
| «список, **как бд красивая** или в форме ленты» | Таблица названа первой — она не довесок к сетке. Делаем оба вида переключателем. | §10 |
| «Дизайн возьми с **пинтереста**, цвета и вайб, **в стиле pifpafai.com**» | Пинтерест — за композицию и сетку, pifpafai.com — за цвета и тон. | §11 |
| «**личные кабинеты**… разные аккаунты с разными рилсами» | Настоящая аутентификация + изоляция данных по `user_id`. | §4 |
| «**они туда грузят**» | Добавление инициирует блогер, вставкой ссылки. | §5 |
| «Красивый формат **дашборда. Аналитики и личного кабинета**» | Три названных экрана, не два. «Личный кабинет» (профиль, настройки) — отдельный пункт ТЗ. | §10 |
| «**джаваскрипт, скл**» | Хотят увидеть **реальные SQL-запросы**, а не ORM-магию. Агрегаты и оконные функции. | §8 |
| «Важно! **задеплоить сайт и показывать сайт а не гит**» | Прод должен открыться у них с телефона, из России, без прогрева. Проверят один раз. | §13 |
| «Смотрим на **детали и на очеловеченность**» | Ключевой критерий. Продукт, а не CRUD-скелет. | §12 |

> ### ⚠️ «бесплатки хватит» — это требование
>
> Фраза читается как «не переживай о деньгах», а означает противоположное:
> **объём задачи такой, что бесплатного тарифа достаточно — если не уложился,
> значит спроектировал неправильно.**
>
> Наивный крон «раз в час на все рилсы» сжигает free tier Apify за полтора дня
> (расчёт в §1) и проваливает явно озвученное условие ровно к моменту проверки.
> Адаптивное расписание из §7 — не инициатива, а прямой ответ на ТЗ.
> **Это стоит отдельной строкой в сопроводительном сообщении.**

### Чего в ТЗ нет — и что мы всё-таки достраиваем

Честное разделение, чтобы приоритеты не поплыли:

| Что | Статус | Обоснование |
|---|---|---|
| **История метрик и графики роста** | Обоснованная достройка, **ядро продукта** | Буквально требуется только «обновляется». Но «дашборд, аналитика» без оси времени — это табло, а не аналитика. Без снапшотов нечего показывать на графике. Делаем. |
| Лайки и комментарии, ER% | Дешёвый бонус | В ТЗ не названы, но приходят тем же ответом Apify. |
| **Админ-роль и лидерборд блогеров** | **Бонус, режется первым** | В ТЗ нет ни слова. Хорошо показывает SQL, но это придуманная область. Не делать в ущерб названному. |
| Экспорт CSV, тёмная тема | Бонус | Из §12, «раз дедлайна нет». |

Обратная сторона: «**внутренних** блоггеров» намекает, что аккаунты в реальности
заводил бы админ, а не открытая регистрация. Но проверяющему нужно уметь
зарегистрироваться самому → оставляем открытую регистрацию плюс кнопку «Войти как демо».

**Кто проверяет:** в футере pifpafai.com — ИП Золотых Сергей Сергеевич. Это тот же
человек, чей телеграм указан для сдачи. Смотрит основатель, а не тимлид → приоритет
у продуктового ощущения, а не у чистоты архитектуры.

**Их стек (видно из разметки сайта):** Next.js, хранилище — Selectel S3
(`ru-1.storage.selcloud.ru`), Яндекс.Метрика, `theme-color: #eef4ff`.

---

## 1. Бюджет бесплатных тарифов — считаем до первой строчки кода

Это раздел, из которого следует архитектура. Не пропускать.

Двойное основание: наше собственное ограничение «0 ₽» **и** прямая фраза из ТЗ
«бесплатки хватит» (см. врезку в §0). Выход за free tier — это не перерасход,
это невыполненное условие задачи.

### Apify

| Параметр | Значение |
|---|---|
| Кредиты | **$5/мес**, не переносятся на следующий месяц |
| Актор `apify/instagram-scraper` | **$2.70 / 1000 результатов** на free-тарифе |
| Один результат | Один спарсенный рилс |
| **Потолок** | **≈1 850 синхронизаций в месяц** |

Наивный крон «раз в час на все рилсы» стоит:

```
60 рилсов × 24 синка/сутки × 30 дней = 43 200 результатов ≈ $117/мес
```

В 23 раза выше лимита — кредиты сгорят за полтора дня, после чего Apify блокирует
запуски и сайт встанет ровно тогда, когда его откроет проверяющий.
**Поэтому частота опроса адаптивная, см. §7.**

Распределение месячного бюджета в 1 850 результатов:

| Статья | Результатов | Обоснование |
|---|---|---|
| Разработка и отладка | 300 | Основная работа — на фикстурах, живые вызовы только для сверки |
| Фоновый синк (адаптивный) | ~900 | См. расчёт в §7 |
| Действия проверяющего | 200 | Добавления рилсов + ручные «Обновить» |
| **Резерв** | **~450** | На повторы после ошибок актора |

### Neon (PostgreSQL)

0.5 ГБ хранилища на проект · 100 CU-часов/мес (≈400 часов при 0.25 CU) ·
автосуспенд через 5 минут простоя · **данные не удаляются** при исчерпании лимитов.

Выбран вместо Supabase именно из-за последнего: Supabase на free-тарифе замораживает
проект после недели простоя, а тестовое может пролежать без открытий и дольше.

Оценка расхода: обложки ≈3 МБ, снапшоты при адаптивном синке ≈10 МБ/мес,
пробуждения кроном ≈30 CU-часов/мес из 100.

### Vercel

Hobby: бесплатно, поддомен `*.vercel.app`, крон **не чаще раза в сутки**.
Раз в сутки для адаптивного синка мало → внешний планировщик, см. §13.

---

## 2. Стек

| Слой | Выбор | Почему |
|---|---|---|
| Фреймворк | **Next.js 16 (App Router) + TypeScript** | Фронт и бэк в одном деплое. TS компилируется в JS — требование «джаваскрипт» выполнено, типы читаются как плюс. |
| БД | **PostgreSQL на Neon** | Требование «скл». См. §1 про выбор против Supabase. |
| Доступ к БД | **Drizzle ORM** + сырой SQL для аналитики | Drizzle — SQL-first, нормальные миграции. Тяжёлые запросы пишем руками — их и хотят увидеть. |
| Auth | JWT в httpOnly cookie, **`jose`** + `bcryptjs` | `jose` не зависит от Node API → сохраняет живым запасной деплой на Cloudflare Workers, см. §4 и §13. |
| UI | Tailwind + shadcn/ui + Framer Motion | Быстро и не выглядит как бутстрап 2015 года. |
| Графики | **Recharts** | Ставится за минуту, прилично выглядит из коробки. |
| Данные | **Apify API** | Прямо указано в ТЗ. |
| Обложки | **`bytea` в Postgres** + отдача route handler'ом | Ноль стоимости, ноль привязки к площадке деплоя. См. §6. |
| Обработка картинок | `sharp` | Ресайз в WebP; нужен и для `next/image` вне Vercel. |
| Тесты | `vitest` | Нормализация URL и метрик — чистые функции, покрываются дёшево и окупаются сразу. |
| Деплой | **Vercel** + запасной вариант | См. §13. |

**Не берём:** Prisma (медленные холодные старты на serverless + прячет SQL, который
как раз хотят увидеть), Redis (не нужен), Docker Compose из пяти контейнеров,
внешний объектный сторадж (лишний аккаунт и привязка к площадке).

---

## 3. Архитектура

```
[Блогер] ──вставляет ссылку──> POST /api/reels
                                    │
                                    ├─ нормализация URL → shortcode  (§5)
                                    ├─ дедуп по (user_id, shortcode)
                                    ├─ проверка бюджета Apify        (§7)
                                    ├─ INSERT reels (sync_status='pending')
                                    └─ запуск Apify run (async) ──> sync_runs.apify_run_id
                                              │
                        ┌─────────────────────┴──────────────────┐
                        │                                        │
              Apify webhook → /api/apify/webhook        поллинг с фронта (fallback)
                        │                                        │
                        └─────────────────┬──────────────────────┘
                                          │
                        dataset items → нормализация метрик (§6)
                                          │
                        ├─ UPDATE reels (caption, posted_at, ...)
                        ├─ INSERT reel_snapshots — ТОЛЬКО если метрики изменились
                        ├─ UPDATE reels.last_synced_at — ВСЕГДА
                        └─ обложка → sharp → WebP → reel_thumbnails.data (bytea)

[Внешний крон каждые 30 мин] ──> POST /api/cron/sync
                                    ├─ выбирает рилсы, у которых подошёл срок (§7)
                                    ├─ сверяется с месячным бюджетом Apify
                                    └─ один батч-run на все отобранные URL
```

Три ключевые идеи:

1. **`reels` — сущность, `reel_snapshots` — временной ряд.** Без снапшотов не будет
   ни графиков, ни слова «обновляется».
2. **Снапшот пишется только при изменении метрик**, а `last_synced_at` обновляется
   всегда. Иначе при частом опросе график роста превращается в лестницу из одинаковых
   значений, а таблица пухнет на сотни строк в сутки. Свежесть данных доказывается
   полем `last_synced_at`, а не дублирующей строкой в истории.
3. **Частота опроса зависит от возраста рилса**, а не фиксирована.

---

## 4. Auth и перехват запросов в Next.js 16

> **Правка от 2026-08-23.** Ранее в этом разделе было написано, что middleware
> исполняется в Edge Runtime и `jsonwebtoken` там не запустится. **Для Next.js 16
> это неверно** — проверено по `node_modules/next/dist/docs/`, а не по памяти.
> Ниже актуальная картина.

Из истории версий в документации:

```
v15.2.0  Middleware can now use the Node.js runtime (experimental)
v15.5.0  Middleware can now use the Node.js runtime (stable)
v16.0.0  Middleware deprecated → Proxy. Proxy defaults to the Node.js runtime
```

Что из этого следует:

- **`middleware.ts` устарел и переименован в `proxy.ts`.** Экспортируемая функция —
  `proxy`, не `middleware`. Автомиграция: `npx @next/codemod@canary middleware-to-proxy .`
- **Proxy по умолчанию исполняется в Node.js-рантайме.** Опция `runtime` в proxy-файле
  недоступна и **бросает ошибку** при попытке её задать.
- Значит `jsonwebtoken` и `bcryptjs` там технически работают. Ограничения Edge больше нет.

**Почему `jose` всё равно, а не `jsonwebtoken`:** он не зависит от Node API вообще.
Это единственное, что сохраняет живым запасной деплой на Cloudflare Workers из §13,
где Node-рантайма нет. Обоснование сменилось с «иначе упадёт» на «иначе теряем
запасную площадку», решение осталось прежним.

**Почему `bcryptjs` всё равно не тащим в proxy:** он там не нужен — proxy проверяет
подпись токена, а не пароль. Документация прямо просит держать proxy автономным:
он может исполняться вне основного рантайма приложения, в том числе на CDN.

Разделение ответственности:

| Где | Рантайм | Что делает |
|---|---|---|
| `src/proxy.ts` | Node (по умолчанию) | Верифицирует JWT из httpOnly cookie, редиректит неавторизованных с `/app/*` |
| `/api/auth/*` | Node | `bcryptjs.hash` / `compare`, выпуск токена через `jose` |
| Любой `/api/*` с данными | Node | `WHERE user_id = $session.userId` — без исключений |

> **Без `matcher` proxy выполняется на каждом запросе** — включая `_next/static`,
> оптимизацию картинок и файлы из `public/`. Логика авторизации без матчера
> заблокирует загрузку собственных CSS и JS. Матчер обязателен.

**Тест изоляции обязателен:** залогиниться под демо-1, запросить `/api/reels/:id`
чужого рилса → **404**, не 200 и не 403 (403 подтверждает факт существования записи).

---

## 5. Приём ссылки: нормализация URL

Это первое, что сделает проверяющий, и делать он это будет **копированием с телефона**.
Поддержать нужно все реальные формы, а не только каноническую.

| Форма | Пример |
|---|---|
| Каноническая | `https://www.instagram.com/reel/{code}/` |
| Множественное число | `https://www.instagram.com/reels/{code}/` |
| Пост | `https://instagram.com/p/{code}/` |
| IGTV (legacy) | `https://www.instagram.com/tv/{code}/` |
| С автором в пути | `https://www.instagram.com/{username}/reel/{code}/` |
| Хвост от «Поделиться» | `...?igsh=MzRlODBiNWFlZA==&img_index=1` |
| Без протокола | `instagram.com/reel/{code}` |
| С мусором по краям | вставка из буфера часто тащит пробелы и `\n` |

Реализация: чистая функция `normalizeReelUrl(input: string)` →
`{ ok: true, shortcode, canonicalUrl } | { ok: false, reason }`,
где `reason` — **человеческий текст** для интерфейса, а не код ошибки.
Покрывается юнит-тестами построчно по таблице выше: 20 минут работы,
а цена промаха — «у меня ссылка не принялась» в первую минуту демо.

Shortcode: `[A-Za-z0-9_-]{5,20}`. Отдельно отсекать служебные сегменты
(`explore`, `stories`, `accounts`, `direct`), чтобы `instagram.com/explore/` не
превратился в рилс с shortcode `explore`.

---

## 6. Данные и обложки

### Нормализация метрик

Поля актора `apify/instagram-scraper` для рилса: `id`, `shortCode`, `url`, `caption`,
`likesCount`, `commentsCount`, `videoViewCount`, `videoPlayCount`, `videoDuration`,
`displayUrl`, `ownerUsername`, `timestamp`, `productType` (`"clips"` у рилсов),
`dimensionsHeight`, `dimensionsWidth`, `musicInfo`.

> `videoViewCount` и `videoPlayCount` — **разные метрики**, и одна из них часто `null`.
> Логика: `views = videoPlayCount ?? videoViewCount ?? null`.
> Это ровно та «деталь», на которую смотрят. Покрыть тестом, включая случай,
> когда обе `null` — интерфейс должен показать «нет данных», а не `0`.

**Первым делом, до всякого кода:** прогнать актор в консоли Apify на 3–5 реальных
ссылках и посмотреть на JSON глазами. Форматы у акторов меняются; гадать по
документации — потеря часа. Сохранить ответы в `fixtures/` — это и среда разработки
(`APIFY_MOCK=1`), и план Б, если кредиты кончатся.

### 🔴 Главная ловушка: обложки Instagram

CDN-ссылки Instagram (`*.cdninstagram.com`) — **подписанные и протухающие**, плюс
блокируют хотлинк с чужих доменов. Если положить `displayUrl` прямо в `<img src>`:

- локально всё покажется (браузер закеширует),
- **у проверяющего через сутки будет сетка пустых серых квадратов.**

Это самый вероятный способ провалить тест «на детали».

**Решение:** при первой синхронизации скачиваем обложку на сервере, гоним через
`sharp` (ресайз до 480px по ширине, WebP, качество 80 → ~30–40 КБ) и кладём в
таблицу `reel_thumbnails` как `bytea`. Отдаём своим route handler'ом
`/api/thumbnails/[shortcode]` с `Cache-Control: public, max-age=31536000, immutable`.

Почему в Postgres, а не в объектный сторадж: нулевая стоимость, ни одного лишнего
аккаунта, переживает переезд деплоя на другую площадку. 60 рилсов ≈ 3 МБ при лимите
Neon в 0.5 ГБ. **Компромисс честно описываем в README** — в продакшене это S3/Selectel,
здесь так сделано ради нулевого бюджета и переносимости.

Страховка: компонент `<ReelCover>` с `onError` → плейсхолдер с градиентом и первой
буквой автора. Никогда не показывать сломанную иконку картинки.

---

## 7. Адаптивная синхронизация

Просмотры рилса растут по кривой с насыщением: взрывной рост первые 24–48 часов,
затем плато. Опрашивать месячный рилс раз в час — платить за 24 идентичных ответа
в сутки. Отсюда расписание по возрасту публикации:

| Возраст рилса | Интервал опроса | Синков в месяц на рилс |
|---|---|---|
| < 48 часов | 1 час | ~48 (за первые двое суток) |
| 2–7 дней | 6 часов | ~20 |
| 7–30 дней | 24 часа | ~23 |
| > 30 дней | 7 дней | ~4 |

Расчёт для демо-набора в 60 рилсов, где большинство старше месяца:

```
50 рилсов старше 30 дней  × 4  = 200
 8 рилсов возрастом 7–30 дней × 23 = 184
 2 свежих рилса             × 68 = 136
                            итого ≈ 520 результатов/мес
```

Укладывается в выделенные ~900 с запасом.

### Защита бюджета в коде

Одного расписания мало — нужен предохранитель, иначе ошибка в логике выборки
за ночь сожжёт месячный лимит.

- Таблица `apify_usage`: счётчик результатов по месяцам.
- Перед каждым запуском — проверка `monthly_used + batch_size <= APIFY_MONTHLY_CAP`
  (в env, по умолчанию 1500 — ниже реального потолка 1850, чтобы был буфер).
- При исчерпании: синк не запускается, в интерфейсе честная плашка
  «Лимит обновлений на этот месяц исчерпан, данные от {дата}».
- Ручное «Обновить» — троттлинг: не чаще раза в час на рилс, не чаще N в минуту
  на пользователя.

Счётчик расхода стоит показать в админ-разделе. Проверяющий увидит, что автор
думал о стоимости эксплуатации — это редкость на тестовых.

---

## 8. Схема БД

```sql
CREATE TABLE users (
  id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email            TEXT UNIQUE NOT NULL,
  password_hash    TEXT NOT NULL,
  display_name     TEXT NOT NULL,
  instagram_handle TEXT,
  avatar_url       TEXT,
  role             TEXT NOT NULL DEFAULT 'blogger',  -- 'blogger' | 'admin'
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE reels (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  shortcode       TEXT NOT NULL,
  url             TEXT NOT NULL,
  caption         TEXT,
  owner_username  TEXT,
  thumbnail_src   TEXT,             -- оригинальный CDN-URL, только для отладки
  posted_at       TIMESTAMPTZ,
  duration_sec    NUMERIC(6,2),
  sync_status     TEXT NOT NULL DEFAULT 'pending', -- pending|ok|failed|unavailable
  sync_error      TEXT,
  last_synced_at  TIMESTAMPTZ,      -- обновляется ВСЕГДА при успешном опросе
  next_sync_at    TIMESTAMPTZ,      -- вычисляется по возрасту, см. §7
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (user_id, shortcode)
);

-- Временной ряд. Строка пишется ТОЛЬКО если метрики отличаются от предыдущей.
CREATE TABLE reel_snapshots (
  id          BIGSERIAL PRIMARY KEY,
  reel_id     UUID NOT NULL REFERENCES reels(id) ON DELETE CASCADE,
  views       BIGINT,
  plays       BIGINT,
  likes       BIGINT,
  comments    BIGINT,
  captured_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Обложки: отдельная таблица, чтобы SELECT * по reels не тащил мегабайты.
CREATE TABLE reel_thumbnails (
  reel_id     UUID PRIMARY KEY REFERENCES reels(id) ON DELETE CASCADE,
  data        BYTEA NOT NULL,
  mime        TEXT  NOT NULL DEFAULT 'image/webp',
  width       INT,
  height      INT,
  bytes       INT   NOT NULL,
  fetched_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE sync_runs (
  id            BIGSERIAL PRIMARY KEY,
  reel_id       UUID REFERENCES reels(id) ON DELETE CASCADE,
  apify_run_id  TEXT,
  status        TEXT NOT NULL,     -- running|succeeded|failed
  error         TEXT,
  started_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  finished_at   TIMESTAMPTZ
);

-- Предохранитель бюджета Apify, см. §7.
CREATE TABLE apify_usage (
  period      TEXT PRIMARY KEY,    -- 'YYYY-MM'
  results     INT NOT NULL DEFAULT 0,
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_reels_user        ON reels(user_id);
CREATE INDEX idx_reels_due         ON reels(next_sync_at) WHERE sync_status <> 'unavailable';
CREATE INDEX idx_snapshots_reel_ts ON reel_snapshots(reel_id, captured_at DESC);
```

Обратить внимание на две детали схемы:

- **`reel_thumbnails` вынесена из `reels`.** Держать `bytea` в основной таблице —
  значит тащить мегабайты в каждом запросе ленты. Postgres хранит крупные значения
  в TOAST отдельно, но `SELECT *` их всё равно распакует.
- **Частичный индекс `idx_reels_due`** исключает мёртвые рилсы (приватные/удалённые)
  из выборки крона — их незачем опрашивать вечно.

### SQL, который стоит написать руками (это витрина)

Вынести в `src/db/queries/` отдельными файлами с комментариями. Если проверяющий
откроет репозиторий, он должен сразу увидеть осознанный SQL.

**Последний снапшот по каждому рилсу — `DISTINCT ON`:**

```sql
SELECT DISTINCT ON (r.id)
       r.id, r.shortcode, r.caption, r.posted_at,
       s.views, s.likes, s.comments, s.captured_at
FROM reels r
LEFT JOIN reel_snapshots s ON s.reel_id = r.id
WHERE r.user_id = $1
ORDER BY r.id, s.captured_at DESC;
```

`DISTINCT ON` — специфичный для Postgres синтаксис, который планировщик закрывает
одним `Index Scan` по `(reel_id, captured_at DESC)`, без материализации всех строк.
Вариант через `ROW_NUMBER() OVER (...) WHERE rn = 1` делает то же самое, но дороже.

**Прирост просмотров за 7 дней — оконная функция:**

```sql
WITH ranked AS (
  SELECT s.reel_id, s.views, s.captured_at,
         ROW_NUMBER() OVER (PARTITION BY s.reel_id ORDER BY s.captured_at DESC) AS rn_new,
         ROW_NUMBER() OVER (PARTITION BY s.reel_id ORDER BY s.captured_at ASC)  AS rn_old,
         COUNT(*)    OVER (PARTITION BY s.reel_id)                              AS points
  FROM reel_snapshots s
  JOIN reels r ON r.id = s.reel_id
  WHERE r.user_id = $1 AND s.captured_at > now() - INTERVAL '7 days'
)
SELECT reel_id,
       CASE WHEN MAX(points) > 1
            THEN MAX(views) FILTER (WHERE rn_new = 1)
               - MAX(views) FILTER (WHERE rn_old = 1)
       END AS growth_7d          -- NULL, если точка всего одна
FROM ranked
GROUP BY reel_id
ORDER BY growth_7d DESC NULLS LAST;
```

> `CASE WHEN MAX(points) > 1` — не косметика. Без него рилс с единственным снапшотом
> в окне даст `rn_new = rn_old = 1`, то есть прирост `0`, и интерфейс покажет «+0»
> там, где честный ответ — «данных пока мало». Показывать `—`, а не ноль.

**Динамика суммарных просмотров по дням (для графика):**

```sql
SELECT date_trunc('day', s.captured_at AT TIME ZONE 'Europe/Moscow') AS day,
       SUM(s.views) AS total_views
FROM reel_snapshots s
JOIN reels r ON r.id = s.reel_id
WHERE r.user_id = $1
GROUP BY 1
ORDER BY 1;
```

**Лидерборд блогеров (админ-режим):**

```sql
WITH latest AS (
  SELECT DISTINCT ON (s.reel_id) s.reel_id, s.views, s.likes, s.comments
  FROM reel_snapshots s ORDER BY s.reel_id, s.captured_at DESC
)
SELECT u.display_name,
       COUNT(r.id)                                        AS reels_count,
       COALESCE(SUM(l.views), 0)                          AS total_views,
       ROUND(AVG(l.views))                                AS avg_views,
       ROUND(100.0 * SUM(l.likes + l.comments)
             / NULLIF(SUM(l.views), 0), 2)                AS er_percent
FROM users u
LEFT JOIN reels r  ON r.user_id = u.id
LEFT JOIN latest l ON l.reel_id = r.id
GROUP BY u.id
ORDER BY total_views DESC;
```

`NULLIF(SUM(l.views), 0)` спасает от деления на ноль у блогера без просмотров —
без него весь лидерборд падает с ошибкой из-за одного пустого аккаунта.

### Таймзона — отдельно и обязательно

`posted_at` и `captured_at` — `TIMESTAMPTZ`, Postgres хранит их в UTC.
`date_trunc('hour', posted_at)` даст **UTC-часы**, и гистограмма «когда лучше постить»
уедет на −3 часа. Проверяющий из индустрии знает, что рилсы заходят вечером;
график с пиком в 17:00 вместо 20:00 прочитается как сломанный.

Везде, где время попадает в интерфейс или в группировку, писать
`AT TIME ZONE 'Europe/Moscow'`. Завести константу, не размазывать литерал по коду.

---

## 9. API-эндпоинты

```
POST   /api/auth/register      { email, password, displayName }
POST   /api/auth/login         { email, password }        → httpOnly cookie
POST   /api/auth/logout
POST   /api/auth/demo          вход в демо-аккаунт одним кликом

GET    /api/reels              ?sort=views|date&range=7d&q=      (только свои)
POST   /api/reels              { url }                    → 202 + reel в статусе pending
GET    /api/reels/:id          полная карточка + история снапшотов
POST   /api/reels/:id/sync     ручное обновление (с троттлингом)
DELETE /api/reels/:id

GET    /api/thumbnails/:shortcode   обложка из bytea, immutable-кеш

GET    /api/stats/overview     KPI-карточки
GET    /api/stats/timeseries   ?range=7d|30d|all           данные для графика
GET    /api/stats/posting-time гистограмма по времени публикации (MSK!)
GET    /api/stats/leaderboard  только для role='admin'
GET    /api/stats/usage        расход кредитов Apify, только для admin

POST   /api/cron/sync          защищён CRON_SECRET в заголовке
POST   /api/apify/webhook      защищён секретом в query
```

**Проверка владения на каждом эндпоинте:** `WHERE user_id = $session.userId`.

---

## 10. Экраны

### `/` — лендинг
Короткий, в стиле pifpafai.com: заголовок, одно предложение, скриншот дашборда,
кнопка «Войти» и «Демо-доступ» (кнопка сама логинит в демо — экономит проверяющему
30 секунд, и это заметят).

### `/app` — дашборд
- Строка KPI: всего рилсов · суммарные просмотры · средние просмотры · ER% · прирост за 7 дней (со стрелкой и цветом)
- Большой график динамики просмотров (Recharts, area chart, переключатель 7д/30д/всё)
- Топ-3 рилса карточками
- Блок «Когда лучше постить» — гистограмма по дням недели и времени публикации
  **в московском времени** (см. §8). Дешёвая фича, а выглядит как продуктовая аналитика.

### `/app/reels` — лента
ТЗ: «список, **как бд красивая** или в форме ленты». Союз «или» разрешает одно из двух,
делаем оба переключателем — но таблица названа первой, поэтому она полноценный вид,
а не довесок к сетке.

- Переключатель вида: **сетка** (Pinterest-стиль, обложки 9:16) ↔ **таблица** (плотная, с сортировкой по любой колонке)
- Поле «вставь ссылку» всегда сверху, с кнопкой «Вставить из буфера»
- Сортировка, поиск по подписи, фильтр по периоду
- Карточка: обложка, просмотры крупно, лайки/комменты мелко, дата относительная, бейдж роста, кнопки «Обновить» / «Открыть в Instagram»

### `/app/reels/:id` — карточка рилса
Крупная обложка, все метрики, **график роста просмотров по снапшотам**, подпись,
лог синхронизаций, отметка «обновлено N минут назад». Это экран, который продаёт
всю идею «обновляется».

### `/app/settings` — личный кабинет
**Названо в ТЗ отдельным пунктом** («дашборда. Аналитики и личного кабинета»),
поэтому не техническая страница с двумя инпутами, а полноценный экран:
аватар, имя, инстаграм-хендл, смена пароля, сводка «твои цифры за всё время»,
дата регистрации, количество рилсов.

### `/app/team` — только админ · 🎁 БОНУС
Лидерборд блогеров, сравнение, суммарные цифры, расход кредитов Apify.

**В ТЗ этого нет.** Режется первым, если конкурирует за время с чем-то названным.
Ценность — показывает нетривиальный SQL (`DISTINCT ON` + агрегаты по всем юзерам)
и то, что автор подумал об эксплуатации (счётчик кредитов).

---

## 11. Дизайн

**За чем идти на pifpafai.com (10 минут, не больше):**
- Пипеткой в DevTools снять: цвет кнопок, градиенты, радиус скруглений, тени, семейство шрифта.
- Базовый фон холодный светлый — в разметке `theme-color: #eef4ff`.
- Тон текста: короткие фразы, «ты», умеренные эмодзи, «Поехали», «одной кнопкой»,
  «за 10 секунд». Скопировать этот голос в микрокопирайт — это буквально то,
  что называют «очеловеченностью».

**За чем идти на Pinterest (15 минут):**
Запросы: `analytics dashboard ui`, `creator dashboard design`, `social media analytics ui`.
Сохранить 4–5 референсов, вытащить из них **три вещи**: сетку/отступы, работу с
типографикой (крупные цифры + мелкие подписи), карточную композицию.
Не повторять референс попиксельно — это ловушка времени.

**Токены зафиксировать сразу в `globals.css`:**

```css
:root {
  --bg: #eef4ff;
  --surface: #ffffff;
  --ink: #0f172a;
  --muted: #64748b;
  --accent: /* с их сайта */;
  --radius: 20px;
  --shadow: 0 8px 32px rgba(15, 23, 42, .08);
}
```

---

## 12. «Очеловеченность» — чек-лист

Отдельный оцениваемый критерий. Проходить сверху вниз, каждый пункт — 5–15 минут.

**Состояния**
- [ ] Пустое состояние с иллюстрацией и примером ссылки `instagram.com/reel/Cxxxxxx/`
- [ ] Skeleton-загрузка вместо спиннеров
- [ ] Optimistic UI: карточка появляется мгновенно в статусе «Забираем данные…» с пульсацией
- [ ] Состояние ошибки на карточке: «Рилс приватный или удалён» + кнопка «Повторить»
- [ ] Состояние «лимит обновлений исчерпан» с датой последних данных (см. §7)

**Тексты и числа**
- [ ] Ошибки человеческим языком, без `500` и `Error: fetch failed`
- [ ] Числа: `1,2 млн`, `245 тыс.`, `8 431` (пробел-разделитель)
- [ ] `—` вместо `0`, когда данных нет (см. §6 про `null` и §8 про `growth_7d`)
- [ ] Даты: «3 дня назад», точная дата в тултипе, **московское время**
- [ ] Русская типографика: тире, неразрывные пробелы, кавычки-ёлочки

**Мелочи, которые замечают**
- [ ] Дедуп: «Этот рилс уже добавлен» + скролл к существующей карточке
- [ ] Валидация ссылки на клиенте с подсказкой, что именно не так (см. §5)
- [ ] Кнопка «Вставить из буфера» (`navigator.clipboard.readText`)
- [ ] Toast'ы на успех и ошибку
- [ ] Подтверждение при удалении
- [ ] Favicon, `<title>` на каждой странице, OG-картинка (ссылку кинут в ТГ — превью увидят все)
- [ ] Кастомные 404 и 500
- [ ] Плавное появление карточек (`stagger` в Framer Motion)
- [ ] Hover-состояния везде, где кликабельно
- [ ] Мобильная вёрстка — **обязательно**, блогеры сидят с телефона, и проверять будут тоже с телефона

**Раз дедлайна нет — доводим и это**
- [ ] Тёмная тема
- [ ] Клавиатура: `Enter` отправляет ссылку, `Esc` закрывает модалку
- [ ] `prefers-reduced-motion`
- [ ] Экспорт в CSV
- [ ] Юнит-тесты на `normalizeReelUrl` и нормализацию метрик
- [ ] Тест изоляции данных между пользователями (см. §4)

---

## 13. Деплой

**Основной вариант: Vercel + Neon.** Бесплатно, деплой из коробки, поддомен
`*.vercel.app` сразу. Домен не покупаем.

**Риск номер один:** доступность Vercel из России бывает нестабильной, а проверять
будут из России. **Это проверяется эмпирически и в самом начале:** задеплоить пустой
Next.js и открыть с мобильного интернета без VPN. Если не открывается — переезжаем,
пока проект пустой, а не когда в нём двадцать экранов.

**Бесплатные запасные площадки:**
- **Cloudflare Pages / Workers** — бесплатно, но App Router с серверными роутами
  требует `@opennextjs/cloudflare`, это дополнительная возня.
- **Netlify** — бесплатно, adapter для Next.js есть.
- **Render free** — засыпает после 15 минут простоя, холодный старт ~50 секунд.
  Для «проверят один раз» это плохо: есть шанс, что проверяющий увидит белый экран.

**Обновление по расписанию.** Vercel Hobby разрешает крон раз в сутки — для
адаптивного синка мало. Берём **cron-job.org** (бесплатно, интервал от минуты) и
дёргаем `/api/cron/sync` с секретом в заголовке раз в 30 минут. Плюс кнопка
«Обновить» в интерфейсе для запуска на глазах у проверяющего.

**Переменные окружения:**
```
DATABASE_URL=
APIFY_TOKEN=              # ТОЛЬКО на сервере, никогда не в NEXT_PUBLIC_*
APIFY_ACTOR_ID=apify~instagram-scraper
APIFY_MONTHLY_CAP=1500    # предохранитель, ниже реального потолка 1850
APIFY_MOCK=0              # 1 — работать на фикстурах
JWT_SECRET=
CRON_SECRET=
NEXT_PUBLIC_APP_URL=
```

---

## 14. Демо-данные

Самая частая ошибка на таких тестовых: проверяющий заходит, видит форму регистрации,
регистрируется, видит пустой экран, добавляет один рилс, ждёт 40 секунд — и закрывает.
Дашборд с одной точкой на графике не производит впечатления.

**Что нужно:**

1. Два-три демо-аккаунта с 15–25 реальными рилсами.
2. **Сгенерированная история снапшотов за 30 дней** — правдоподобная кривая роста
   (быстрый рост первые 48 часов, потом плато). Писать сидом, не руками.
3. **Кривая должна заканчиваться ровно на текущем реальном значении из Apify.**
   Иначе первая же живая синхронизация нарисует на графике вертикальный обрыв,
   и правдоподобность истории рассыплется на глазах у проверяющего.
4. Кнопка «Войти как демо» на лендинге — один клик, без ввода пароля.
5. Один аккаунт с ролью `admin`, чтобы был виден лидерборд.

Честно указать в README, что исторические снапшоты сгенерированы для демонстрации —
данные с Apify приходят реальные, но за короткий срок истории не накопить. Это как раз
и есть «очеловеченность»: показать, что понимаешь ограничение, и обойти его красиво.

---

## 15. Порядок работ

Дедлайна нет, поэтому вместо часов — вертикальные срезы. Правило одно:
**после каждого среза приложение работает и задеплоено.** Не начинать следующий,
пока предыдущий не доведён.

| № | Срез | Готово, когда |
|---|---|---|
| 0 | **Разведка и каркас.** Прогон актора Apify руками, фикстуры. Neon-БД. Пустой Next.js в проде. **Проверка с телефона из России.** | Пустая страница открывается по публичному URL с мобильного |
| 1 | **Фундамент.** Схема БД, миграции. Auth на `jose` + `proxy.ts`. Тест изоляции. | Можно зарегистрироваться и попасть в `/app`, чужие данные недоступны |
| 2 | **Нормализация ссылок.** `normalizeReelUrl` + юнит-тесты по таблице §5. | Все восемь форм ссылки из §5 дают верный shortcode |
| 3 | **Ядро.** `POST /api/reels`: запуск Apify, поллинг, нормализация метрик, запись снапшота. | Вставил ссылку → в БД появились реальные данные |
| 4 | **Обложки.** Скачивание, `sharp` → WebP → `bytea`, route handler, плейсхолдер. | Обложки открываются со своего домена и не протухают |
| 5 | **Лента.** Список: сетка + таблица, карточка, сортировка, удаление. | Кабинет выглядит как продукт |
| 6 | **Дашборд.** KPI, график динамики, топ-рилсы, карточка рилса с графиком роста. | Аналитика на месте |
| 7 | **Синхронизация.** Адаптивное расписание, предохранитель бюджета, cron-job.org, кнопка «Обновить». | Метрики обновляются сами, кредиты не горят |
| 8 | **Полировка.** §12 сверху вниз. Дизайн-проход. Мобилка. Тёмная тема. | Чек-лист пройден |
| 9 | **Демо и сдача.** Сиды с историей, README, проверка с чужого устройства в инкогнито, видео. | Всё работает у постороннего |

---

## 16. План Б

| Что пошло не так | Действие |
|---|---|
| Кончились кредиты Apify | `APIFY_MOCK=1` + плашка в интерфейсе. Сайт продолжает работать на последних данных. Предохранитель из §7 должен не допустить этого. |
| Актор не отдаёт просмотры | Попробовать другой актор из Apify Store, либо отдавать `null` с корректным «нет данных». |
| Vercel недоступен из РФ | Cloudflare Pages через `@opennextjs/cloudflare` или Netlify. Проверить доступность **на срезе 0**, не позже. |
| Neon упёрся в 0.5 ГБ | Почистить старые снапшоты агрегацией: оставить почасовые за неделю, дневные — дальше. |
| Instagram отдаёт капчу через Apify | Это на стороне актора, у него свои прокси. Повторить запуск, попробовать другой актор. |
| Не успевается мультиаккаунт | Не вариант — прямое требование ТЗ. Резать что угодно, кроме auth. |

---

## 17. Работа с Claude Code (это тоже оценивают)

Вакансия про вайбкодинг — значит важен не только результат, но и организация работы с ИИ.

- `CLAUDE.md` в корне: стек, конвенции, схема БД, «не используй Prisma»,
  «все SQL-запросы в `src/db/queries/`», «тексты интерфейса — на русском, на "ты"»,
  «время в интерфейсе — всегда Europe/Moscow».
- Этот `PLAN.md` и план исполнения в `docs/superpowers/plans/` — в репозитории,
  прогресс отмечается чекбоксами.
- Работать вертикальными срезами: «схема + эндпоинт + экран» за раз, а не «весь
  бэкенд, потом весь фронт». Так всегда есть работающее приложение.
- Коммиты осмысленные (`feat: apify sync pipeline`, а не `fix`). Гит смотреть не
  просили, но если откроют — история говорит о тебе больше кода.
- После каждого среза — коммит и деплой. Прод не должен ломаться.

---

## 18. Как сдавать

Отправлять в телеграм **@sergeyzolotykh**. Не просто ссылку — сообщение должно
работать само по себе:

```
Сергей, привет! Тестовое готово — назвал его PifPaf Pulse.

🔗 Сайт: <ссылка>
👤 Демо-доступ: на главной кнопка «Войти как демо»
🎬 Видео-демо на 60 секунд: <ссылка>

Что внутри:
— Вставляешь ссылку на рилс → данные подтягиваются через Apify API
  (просмотры, лайки, комментарии, дата, обложка)
— Обложки кешируются к себе: ссылки Instagram протухают через сутки,
  иначе у вас была бы пустая сетка
— Метрики пишутся в историю снапшотов, поэтому есть графики роста
  по каждому рилсу и по кабинету
— Ты написал «бесплатки хватит» — уложился в неё осознанно: частота
  обновления адаптивная, свежий рилс опрашивается раз в час, месячный —
  раз в неделю. Просмотры всё равно растут только первые пару суток,
  а опрос всего подряд раз в час сжёг бы free tier за полтора дня.
  В админке видно текущий расход кредитов
— Личные кабинеты с изоляцией данных, есть админ-роль с лидербордом
— Postgres, аналитика на сыром SQL (оконные функции, DISTINCT ON)

Что бы делал дальше: <2–3 пункта — командные проекты, интеграция
TikTok/YouTube Shorts, уведомления о вирусных рилсах в телеграм>

Код: <ссылка на GitHub>
```

**Перед отправкой обязательно:**
1. Открыть сайт в инкогнито **с телефона, с мобильного интернета**.
2. Пройти весь путь: лендинг → демо-вход → дашборд → добавление рилса → карточка.
3. Проверить превью ссылки в телеграме (OG-картинка).
4. Прогреть сайт за пять минут до отправки — на бесплатных тарифах холодный старт
   Neon после автосуспенда добавляет секунду-другую.

**Видео на 60 секунд стоит записать.** Если проверяющий откроет сайт с плохим
интернетом или в неудачный момент — видео спасёт впечатление.

---

## 19. Минимальный набор, ниже которого не опускаться

Каждый пункт — прямая строка ТЗ. Если что-то приходится резать, режется то,
чего в этой таблице нет.

| # | Что | Строка ТЗ |
|---|---|---|
| 1 | Регистрация/вход, данные изолированы по пользователям | «личные кабинеты… разные аккаунты с разными рилсами» |
| 2 | Вставка ссылки → реальные данные с Apify: **просмотры, дата, обложка** | «обновляться по ссылке из инсты просмотры, дата, обложка» |
| 3 | Лента: таблица **и** сетка, обложки не битые | «список, как бд красивая или в форме ленты» |
| 4 | Дашборд: KPI + график | «Красивый формат дашборда. Аналитики» |
| 5 | Экран личного кабинета | «…и личного кабинета» |
| 6 | Повторная синхронизация работает и укладывается в free tier | «обновляться» + «бесплатки хватит» |
| 7 | Задеплоено на публичном URL, открывается из России с телефона | «Важно! задеплоить сайт и показывать сайт а не гит» |
| 8 | Цвета и вайб узнаваемо от pifpafai.com | «цвета и вайб, в стиле нашего приложения» |
| 9 | Демо-аккаунт с наполненными данными | — (иначе проверяющий увидит пустой экран) |
| 10 | Аккуратно выглядит на телефоне | — (проверять будут с телефона) |

Всё остальное — бонусы: лайки/комментарии, ER%, лидерборд, тёмная тема, CSV.
