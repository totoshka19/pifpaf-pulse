/**
 * Единственное место, где написан пояс проекта.
 *
 * PLAN.md §8: `TIMESTAMPTZ` хранится в UTC, и `date_trunc` без `AT TIME ZONE`
 * сдвинет данные на три часа. Гистограмма «когда лучше постить» уехала бы
 * с 20:00 на 17:00, и проверяющий из индустрии прочёл бы это как поломку.
 *
 * Срез 6 подставит эту же константу параметром в SQL: `AT TIME ZONE $1`
 * принимает текстовое выражение, поэтому склеивать строку запроса не придётся.
 */
export const MOSCOW_TZ = 'Europe/Moscow'
