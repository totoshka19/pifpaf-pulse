import { describe, expect, it } from 'vitest'

// Временный тест: проверяет, что харнесс запускается и алиас "@/" резолвится.
// Удаляется, как только появятся настоящие тесты (задача 2).
describe('харнесс', () => {
  it('запускается', () => {
    expect(1 + 1).toBe(2)
  })
})
