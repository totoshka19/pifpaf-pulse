# PifPaf Pulse — срез 7: фоновая синхронизация

> **Для исполнителя:** используй `superpowers:subagent-driven-development` (рекомендуется)
> или `superpowers:executing-plans`. Шаги размечены чекбоксами (`- [ ]`).

**Цель:** метрики рилсов обновляются сами, по расписанию, без участия блогера —
и при этом невозможно слить месячный бюджет Apify ни кнопкой, ни циклом
«удалил → вставил заново».

**Архитектура:** двухфазный крон. Каждый тик сначала забирает результаты
прогонов, запущенных прошлым тиком, потом стартует новый батч — и ничего не
ждёт, потому что Netlify обрывает функцию на 10 секундах, а прогон Apify идёт
14–19. Один прогон Apify на весь батч, но по строке `sync_runs` на рилс с общим
`apify_run_id`. Запускает всё планируемая функция Netlify, а не сторонний сервис.

**Тех.стек:** TypeScript · Next.js 16 (App Router) · PostgreSQL на Neon ·
Drizzle ORM · Netlify Scheduled Functions · Apify API · Vitest.

**Спека:** [`2026-08-24-sync-design.md`](../specs/2026-08-24-sync-design.md) —
проект этого среза, согласован. Продуктовая спека — [`PLAN.md`](../../../PLAN.md) §7.

**Предыдущий срез:** [`2026-08-24-dashboard.md`](2026-08-24-dashboard.md) — закрыт и слит в `main`.

---

## Статус: СРЕЗ ЗАКРЫТ — все 12 задач выполнены

`npm test` → **285 passed** (было 278). `npm run test:db` → **129 passed**
(было 96). `npm run build` и `npm run lint` — без ошибок (одно давнее
предупреждение `no-img-element`). `npm run e2e:feed` → 23 проверки,
`npm run e2e:dashboard` → 34, `npm run e2e:cron` → **18**, все зелёные.

Ветка `slice-7-sync`, 21 коммит от `bad3247`. Слияние в `main` — отдельным
шагом: пуш в `main` публикует на прод.

### Живой прогон крона (задача 12, шаг 4)

Единственный живой вызов Apify в срезе. Рилс добавлен на моке, потом
`APIFY_MOCK` временно выключен, рилс просрочен, прогнаны два тика.

| Что проверяли | Что получили |
|---|---|
| `apify_run_id` настоящий, не `mock:` | `CU2ZBeTriNOl9oBA9` ✅ |
| прогон помечен как крон | `triggered_by = cron` ✅ |
| данные настоящие | автор `aabbigeyl`, 930 869 просмотров против 662 153 в фикстуре двухдневной давности |
| счётчик вырос ровно на 1 | `apify_usage` 6 → **7** из 1500 ✅ |
| расписание переставлено | `next_sync_at` = 16:57 того же дня (адаптивная ступень) |

`APIFY_MOCK=1` возвращён на место сразу после проверки.

### Замер времени тика (задача 9, шаг 3)

Десять просроченных рилсов, из них три с настоящими данными фикстуры —
то есть с тремя попытками скачать обложку, потолок `THUMBNAILS_PER_TICK`.

| Батч | Только запуск | Сбор + запуск | Только сбор |
|---|---|---|---|
| 10 | 635 мс | — | **6169 мс** |
| 5 | 632 мс | **4640 мс** | 4091 мс |

Десять не прошли критерий плана (5000–8000 мс = «запас тонкий»), поэтому
`CRON_BATCH_SIZE` снижен до пяти. Замер идёт на `APIFY_MOCK=1`, где Apify
отвечает мгновенно, — живой добавит по круговой задержке на фазу.

**Сверх ожидаемого:** дорога не обложка, а круговая задержка до базы. Тик,
собравший пять рилсов вообще без единой попытки скачать обложку, занял
4091 мс — почти столько же, сколько тик с тремя скачиваниями. Стоимость
линейна по числу рилсов, не по числу картинок.

---

## Отклонения от плана

| Что план говорил | Что сделано и почему |
|---|---|
| `CRON_BATCH_SIZE = 10` «заведомо безопасно» | **Пять.** Замер показал 6169 мс при потолке Netlify в 10 с — меньше двукратного запаса, и запас тратится ещё до прода. План сам предписывал такой пересмотр по результату замера. |
| Скрипт замера сеет `Tick0…Tick9` | Ни один из этих шорткодов не совпадает с тремя шорткодами фикстуры: все десять рилсов уходят в `unavailable`, обложки не скачиваются, замер выходит обманчиво низким. Пересеяно так, чтобы три рилса проходили полный путь приёма. |
| Функция Netlify берёт адрес только из `NEXT_PUBLIC_APP_URL` | Добавлен запасной `process.env.URL`. Проверено грепом: `NEXT_PUBLIC_APP_URL` не используется больше нигде в коде — она живёт только в `.env.local`, который на Netlify не уезжает. Функция возвращала бы 500 на каждом тике, и крон молча не работал бы, а в панели всё выглядело бы исправным. |
| `e2e-cron.mjs` ходит в базу через Drizzle | Через сырой `postgres`. Из `.mjs` Drizzle недоступен: `src/db/index.ts` тянет алиас `@/`, а `export *` не виден сквозь границу с CJS — записанные грабли проекта. Это же и конвенция семейства `e2e:*`. |
| Тик 3 проверяется как `started === 0` | Проверяется, что у НАШЕГО рилса не завелось новых строк `sync_runs`. База общая с продом, и чужой просроченный рилс красил бы проверку по причине, к сценарию не относящейся. |
| Сквозной скрипт печатает аккаунты для ручного удаления | Печатает **и удаляет** — но только свои и только на зелёном прогоне. На красном состояние базы это улика, и она остаётся. Накопление тестовых аккаунтов — записанный хвост проекта, эта правка его и закрывает. |

### Найдено при исполнении, чего в плане не было

**Сквозной скрипт сперва покраснел на верной причине.** Проверка «тик 1
запустил батч» падала с `collected: 1, started: 0`. Дефекта в кроне нет:
`POST /api/reels` заводит РУЧНОЙ прогон и оставляет рилс в `pending`, а тик
идёт фазами «сначала собрать, потом запустить». Фаза сбора подбирала этот
ручной прогон, `ingestReel` переставлял `next_sync_at` в будущее — и к фазе
запуска рилс уже не был просрочен. Насильственный сдвиг просто затирался
законным. Исправлена последовательность в скрипте, не код.

---

## Проверка тестов на зубы

- **Ветки отказа Apify** (раунд правок 2/5). До этого раунда ни одна из
  четырёх веток не выполнялась ни разу ни одним тестом файла. Маркеры
  разведены нарочно: `MOCK_RUN_FAILED` идёт с НЕпустым `datasetId`, а
  `MOCK_NO_DATASET` — со статусом `SUCCEEDED`, потому что обе причины
  проверяются одним условием `status === 'FAILED' || !datasetId`. Бей каждый
  маркер сразу по обеим половинам — мутация одной осталась бы незамеченной.
- **Лимит обложек за тик.** Первая версия теста сажала все четыре кандидата
  под одним шорткодом, то есть в одну группу `byRun`, и доказывала «не больше
  трёх внутри группы». А `thumbnailAttempts` вынесен ДО цикла по группам ради
  МЕЖгруппового свойства — оно оставалось непроверенным. Мутация (перенос
  счётчика внутрь цикла) подтвердила: тест оставался зелёным. Переписано на
  два разных шорткода → две разные группы.
- **`triggered_by` в логе** (задача 11). Проверяются ОБЕ стороны — что в логе
  есть и `cron`, и `manual`. Запрос, отдающий константу вместо колонки,
  покраснеет на одной из них.
- **Порядок `dueReels`.** Тест на сортировку переписан так, чтобы не совпадать
  с порядком вставки: иначе он был бы зелёным при полностью отсутствующем
  `ORDER BY`.

---

## Где проект на старте этого среза

Прочитай [`AGENTS.md`](../../../AGENTS.md) целиком — там таблица граблей, каждая
из которых стоила времени. Ниже только то, что прямо влияет на этот срез.

- Ветка `main`, срез 6 слит. `npm test` → **278**, `npm run test:db` → **96**,
  `npm run e2e:feed` → 23, `npm run e2e:dashboard` → 34.
- `main` опережает `origin/main` на 16 коммитов — **не запушено**. Netlify
  собирает из `main`, поэтому пуш = публикация.

**Готовые кирпичи, которые надо переиспользовать, а не писать заново:**

| Модуль | Что даёт |
|---|---|
| `src/lib/sync/schedule.ts` | `computeNextSyncAt(postedAt, now)` — адаптивное расписание |
| `src/lib/sync/throttle.ts` | `checkManualSync(lastAttemptAt, now)`, `ThrottleVerdict`, `MANUAL_SYNC_COOLDOWN_MS` |
| `src/db/queries/budget.ts` | `tryReserve(count)` — атомарный резерв, `usage()` |
| `src/db/queries/ingest.ts` | `ingestReel(reelId, items, now?)` — приём с сопоставлением по shortcode |
| `src/lib/apify/client.ts` | `startRun(urls[])`, `getRun(runId)`, `getDatasetItems(datasetId)`, `isMock()` |
| `src/db/queries/coerce.ts` | `toNumber`, `toDate`, `rowsOf` для сырого `db.execute` |
| `src/lib/api/respond.ts` | `ok`, `fail`, `handleError` |
| `src/lib/format/plural.ts` | `plural(count, forms)` |

**Что важно знать про существующий конвейер:**

1. `startRun` принимает **массив** URL и шлёт их как `directUrls`.
2. `ingestReel` сопоставляет элементы датасета **по shortcode**, а не по
   порядку, — батч возвращает их вперемешку. Батчинг заложен ещё в срезе 3.
3. `ingestReel` **сам** выставляет `nextSyncAt` при успехе и `nextSyncAt = null`
   при `unavailable`. Крону остаётся только сдвинуть значение на старте, чтобы
   следующий тик не выбрал тот же рилс повторно.
4. Оба пути, создающих прогон (`POST /api/reels` и `POST /api/reels/:id/sync`),
   вставляют строку `sync_runs` **ДО** `startRun` — чтобы упавший запуск не
   оставил рилс осиротевшим в `pending`. Крон обязан делать так же.

---

## Глобальные ограничения

- **Netlify free: 10 секунд на обычную функцию.** Ни одна фаза не ждёт Apify.
- **Планируемой функции Netlify даёт 30 секунд, но логику в неё переносить
  нельзя:** файлы в `netlify/functions/` вне сборки Next.js и не импортируют
  ни `ingestReel`, ни `computeNextSyncAt`, ни `tryReserve`.
- **Бюджет Apify — жёсткое ограничение.** Разработка на `APIFY_MOCK=1`.
  Живые вызовы — только там, где план говорит об этом явно.
- **`APIFY_MOCK=1` не должен трогать счётчик бюджета.** Резерв вызывается как
  `!isMock() && await tryReserve(n)`.
- **ORM — Drizzle, не Prisma.** Аналитические запросы — сырым SQL в
  `src/db/queries/`; обычные выборки — типизированным путём.
- **`db.execute` не приводит типы:** `bigint`, `numeric`, `timestamptz`
  приходят строками. Использовать `src/db/queries/coerce.ts`.
- **Изоляция данных:** каждый пользовательский эндпоинт фильтрует по
  `user_id` из сессии. Чужая запись — 404, не 403.
- **Тексты интерфейса — по-русски, на «ты»**, ошибки без кодов.
- **Всё время в интерфейсе — `Europe/Moscow`** через `MOSCOW_TZ`.
- **`DATABASE_URL` общий у разработки и прода.** Миграция идёт по живой базе.
- **Файлы с top-level `await` или `import.meta` — расширение `.mts`.**
- **Тестовые аккаунты — только на `@example.invalid`**, адреса записывать в отчёт.

---

## Что сознательно НЕ входит

| Что | Куда | Почему |
|---|---|---|
| Плашка «Лимит исчерпан» и счётчик расхода в интерфейсе | срез 8 | решение человека: срез 7 — механика, интерфейс бюджета идёт с дизайн-проходом |
| Вебхуки Apify | не делаем | публичный эндпоинт, проверка подписи, нет локального тестирования — и поллер всё равно нужен как страховка |
| Сторонний cron-сервис | не делаем | проверено: Netlify Scheduled Functions есть на бесплатном тарифе |
| Отзыв сессий, загрузка аватара, тёмная тема | хвосты и срез 8 | не относятся к синхронизации |

---

## Карта файлов

**Создать:**

| Файл | Ответственность |
|---|---|
| `src/lib/sync/rate-limit.ts` | `checkUserRate` — чистая функция лимита на пользователя |
| `src/lib/sync/rate-limit.test.ts` | юнит-тесты к ней |
| `src/db/queries/sync-state.ts` | выборка «кому пора», счётчики попыток по пользователю и по паре |
| `src/db/queries/sync-state.integration.test.ts` | тесты выборки и счётчиков на живой базе |
| `src/lib/sync/run-cron.ts` | обе фазы тика, без HTTP — чтобы тестировать напрямую |
| `src/lib/sync/run-cron.integration.test.ts` | тесты обеих фаз на живой базе и фикстурах |
| `src/app/api/cron/sync/route.ts` | тонкий HTTP-слой: секрет, вызов, отчёт |
| `netlify/functions/sync-cron.mts` | планируемая функция-триггер |
| `scripts/e2e-cron.mjs` | сквозная проверка |

**Изменить:**

| Файл | Что |
|---|---|
| `src/db/schema.ts` | четыре изменения `sync_runs` плюс три индекса |
| `src/db/migrations/` | сгенерированная миграция плюс ручной backfill |
| `src/lib/sync/throttle.ts` | комментарий про ключ `(user_id, shortcode)` |
| `src/app/api/reels/route.ts` | лимит на пользователя, запись новых полей |
| `src/app/api/reels/[id]/sync/route.ts` | то же плюс троттлинг по паре |
| `src/components/reels/sync-log.tsx` | пометка «по расписанию» |
| `src/db/queries/reel-detail.ts` | отдавать `triggeredBy` наружу |
| `netlify.toml` | расписание планируемой функции |
| `package.json` | скрипт `e2e:cron` |
| `PLAN.md`, `AGENTS.md` | статус, грабли, закрытые хвосты |

---

### Задача 1: Разведка — что означает `resultsLimit: 1` при батче

**Это разведка, а не фича.** От ответа зависит, работает ли батчинг вообще:
если `resultsLimit` ограничивает весь прогон, батч из десяти ссылок вернёт
один элемент, и девять рилсов получат `unavailable` вместо метрик. Узнать это
надо **до** того, как размер батча попадёт в код.

**Файлы:** ничего не создаём и не меняем. Результат — ответ и запись в отчёт.

- [ ] **Шаг 1: Посмотреть, что сейчас отправляется**

Прочитай `startRun` в `src/lib/apify/client.ts`. Тело запроса:

```json
{ "resultsType": "posts", "directUrls": [...], "resultsLimit": 1 }
```

- [ ] **Шаг 2: Прочитать схему входа актора — это бесплатно**

Чтение метаданных актора кредитов не тратит. Положи скрипт в корень и удали
после (не коммитить):

```js
// probe-actor.mjs
const token = process.env.APIFY_TOKEN
const actor = process.env.APIFY_ACTOR_ID
const r = await fetch(`https://api.apify.com/v2/acts/${actor}?token=${token}`)
const { data } = await r.json()
console.log('актор:', data.name, 'by', data.username)
const schema = data.taggedBuilds?.latest?.inputSchema ?? data.exampleRunInput?.body
console.log(JSON.stringify(schema ?? data, null, 1).slice(0, 3000))
```

Запуск: `node --env-file=.env.local probe-actor.mjs`

Ищи описание поля `resultsLimit`: сказано ли «per URL» / «for each» или «total».

- [ ] **Шаг 3: Если схема не отвечает однозначно — живой прогон на ДВУХ ссылках**

Это стоит два кредита из ~1850. Возьми два разных шорткода из
`fixtures/apify/`, отправь их **одним** прогоном с `resultsLimit: 1`,
дождись и посчитай элементы датасета.

```js
// probe-batch.mjs
const token = process.env.APIFY_TOKEN
const actor = process.env.APIFY_ACTOR_ID
const urls = [
  'https://www.instagram.com/reel/DcXVbOOiyhL/',
  'https://www.instagram.com/reel/DcJsGN3tYER/',
]
const start = await fetch(`https://api.apify.com/v2/acts/${actor}/runs?token=${token}`, {
  method: 'POST',
  headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ resultsType: 'posts', directUrls: urls, resultsLimit: 1 }),
})
const { data: run } = await start.json()
console.log('прогон', run.id, 'статус', run.status)

for (let i = 0; i < 40; i++) {
  await new Promise((r) => setTimeout(r, 3000))
  const { data } = await (await fetch(`https://api.apify.com/v2/actor-runs/${run.id}?token=${token}`)).json()
  process.stdout.write(`${data.status} `)
  if (data.status !== 'RUNNING' && data.status !== 'READY') {
    const items = await (await fetch(`https://api.apify.com/v2/datasets/${data.defaultDatasetId}/items?token=${token}`)).json()
    console.log('\nэлементов:', items.length)
    console.log('шорткоды:', items.map((i) => i.shortCode))
    break
  }
}
```

- [ ] **Шаг 4: Вынести вердикт и записать в отчёт**

Три возможных исхода и что делать с каждым:

| Исход | Что значит | Действие |
|---|---|---|
| Два элемента, оба шорткода | лимит **на URL** | батч работает как задумано, `resultsLimit: 1` оставить |
| Один элемент | лимит **на прогон** | в задаче 8 поднять `resultsLimit` до размера батча. **Осторожно:** тогда ссылка на профиль вместо рилса снова сможет грести ленту — оставить `resultsLimit` равным ровно длине массива, не больше |
| Ошибка или ноль элементов | актор не принял батч | **BLOCKED, остановиться и сообщить.** Батчинг придётся заменить на прогон-на-рилс, а это меняет задачи 8 и 9 |

Записать: версию/имя актора, что было в схеме, число элементов, сколько
кредитов потрачено.

- [ ] **Шаг 5: Убрать за собой**

```bash
rm -f probe-actor.mjs probe-batch.mjs
```

Коммита в этой задаче нет — ничего не создано.

---

### Задача 2: Миграция `sync_runs`

**Первая миграция с среза 1, и она идёт по ЖИВОЙ базе прода.** Выполнять
осознанно, не в фоне, читая вывод.

**Файлы:**
- Изменить: `src/db/schema.ts`
- Создать: миграция в `src/db/migrations/` (генерируется), плюс ручной backfill

**Интерфейсы:**
- Отдаёт наружу: колонки `syncRuns.userId`, `syncRuns.shortcode`,
  `syncRuns.triggeredBy` — используются задачами 3–8.

- [ ] **Шаг 1: Изменить схему**

В `src/db/schema.ts` заменить объявление `syncRuns` целиком:

```ts
export const syncRuns = pgTable(
  'sync_runs',
  {
    id: bigserial('id', { mode: 'number' }).primaryKey(),
    /**
     * SET NULL, а не CASCADE. Удаление рилса больше не стирает историю его
     * попыток: на ней держатся оба лимита, и обнулять её значит открывать
     * дыру «удалил → вставил заново → троттлинг обойдён».
     */
    reelId: uuid('reel_id').references(() => reels.id, { onDelete: 'set null' }),
    /**
     * Владелец попытки. Пережить удаление рилса обязан — иначе лимит
     * «N в минуту на пользователя» обнуляется тем же удалением.
     */
    userId: uuid('user_id').references(() => users.id, { onDelete: 'cascade' }),
    /**
     * Вместе с user_id образует СТАБИЛЬНУЮ идентичность «копии рилса у этого
     * блогера»: в reels на этой паре стоит uq_reels_user_shortcode. В отличие
     * от reel_id, она переживает удаление и повторную вставку.
     */
    shortcode: text('shortcode'),
    /**
     * Кто инициировал. Без этого прогон крона считался бы попыткой
     * пользователя, и тик, тронувший десять моих рилсов, заблокировал бы мне
     * кнопку «Обновить» на минуту — при том, что я ничего не нажимал.
     */
    triggeredBy: text('triggered_by', { enum: ['manual', 'cron'] })
      .notNull()
      .default('manual'),
    apifyRunId: text('apify_run_id'),
    status: text('status', { enum: ['running', 'succeeded', 'failed'] }).notNull(),
    error: text('error'),
    startedAt: timestamp('started_at', { withTimezone: true }).notNull().defaultNow(),
    finishedAt: timestamp('finished_at', { withTimezone: true }),
  },
  (t) => [
    check('ck_sync_runs_status', sql`${t.status} IN ('running', 'succeeded', 'failed')`),
    check('ck_sync_runs_triggered_by', sql`${t.triggeredBy} IN ('manual', 'cron')`),
    // Троттлинг читает MAX(started_at) по паре — индекс ровно под этот запрос.
    index('idx_sync_runs_pair').on(t.userId, t.shortcode, t.startedAt.desc()),
    // Лимит на пользователя: свежие ручные попытки одного человека.
    index('idx_sync_runs_user_ts').on(t.userId, t.startedAt.desc()),
    // Фаза 1 крона ищет незавершённые прогоны. Частичный: завершённых
    // строк со временем станет на порядки больше, чем висящих.
    index('idx_sync_runs_running')
      .on(t.apifyRunId)
      .where(sql`status = 'running'`),
  ],
)
```

> `userId` и `shortcode` объявлены **nullable** намеренно. Сделать их
> `NOT NULL` можно только вместе с backfill внутри той же миграции, а
> drizzle-kit такого не пишет — пришлось бы править сгенерированный SQL руками
> и рисковать на живой базе. Код с этого момента заполняет их всегда, а
> запросы фильтруют по равенству, что NULL и так не пропускает.
>
> `triggeredBy` — наоборот, `NOT NULL DEFAULT 'manual'`: Postgres проставит
> значение всем существующим строкам сам, и это правда — крона до сих пор
> не было, все прошлые прогоны ручные.

- [ ] **Шаг 2: Сгенерировать миграцию**

```bash
npm run db:generate
```

Прочитать сгенерированный SQL в `src/db/migrations/`. Убедиться, что там:
- `ALTER TABLE sync_runs ADD COLUMN user_id uuid` и внешний ключ на `users`;
- `ADD COLUMN shortcode text`;
- `ADD COLUMN triggered_by text NOT NULL DEFAULT 'manual'`;
- `DROP CONSTRAINT` старого внешнего ключа `reel_id` и `ADD CONSTRAINT`
  с `ON DELETE SET NULL`;
- три `CREATE INDEX`.

Если чего-то нет — **остановиться и разобраться**, а не дописывать руками
наугад.

- [ ] **Шаг 3: Применить**

```bash
npm run db:migrate
```

- [ ] **Шаг 4: Backfill существующих строк**

Положи скрипт в корень, прогони, удали (не коммитить):

```js
// backfill.mjs
import postgres from 'postgres'
const sql = postgres(process.env.DATABASE_URL_UNPOOLED ?? process.env.DATABASE_URL, { prepare: false })

const before = await sql`SELECT count(*) FILTER (WHERE user_id IS NULL) AS empty, count(*) AS total FROM sync_runs`
console.log('до:', before[0])

await sql`
  UPDATE sync_runs s
  SET user_id = r.user_id, shortcode = r.shortcode
  FROM reels r
  WHERE r.id = s.reel_id AND s.user_id IS NULL
`

const after = await sql`SELECT count(*) FILTER (WHERE user_id IS NULL) AS empty, count(*) AS total FROM sync_runs`
console.log('после:', after[0])
await sql.end()
```

Запуск: `node --env-file=.env.local backfill.mjs`, затем `rm -f backfill.mjs`

Оставшиеся строки с `user_id IS NULL` — это прогоны рилсов, удалённых **до**
миграции; их `reel_id` уже обнулился каскадом. Их немного и они безвредны:
запросы фильтруют по равенству.

- [ ] **Шаг 5: Проверить структуру на живой базе**

```bash
npm run db:verify
```

Плюс глазами:

```js
// check.mjs — прогнать и удалить
import postgres from 'postgres'
const sql = postgres(process.env.DATABASE_URL_UNPOOLED ?? process.env.DATABASE_URL, { prepare: false })
console.log(await sql`
  SELECT column_name, data_type, is_nullable, column_default
  FROM information_schema.columns WHERE table_name = 'sync_runs' ORDER BY ordinal_position
`)
console.log(await sql`
  SELECT conname, confdeltype FROM pg_constraint
  WHERE conrelid = 'sync_runs'::regclass AND contype = 'f'
`)
await sql.end()
```

`confdeltype` для ключа на `reels` обязан быть `n` (SET NULL), а не `c` (CASCADE).

- [ ] **Шаг 6: Прогнать существующие тесты**

```bash
npm test
npm run test:db
```

Ожидаем 278 и 96 — миграция ничего не должна сломать.

- [ ] **Шаг 7: Коммит**

```bash
git add src/db/schema.ts src/db/migrations
git commit -m "feat: sync_runs переживает удаление рилса и знает, кто запустил прогон"
```

---

### Задача 3: Существующие пути пишут новые поля

Пока никто не пишет `user_id`, `shortcode` и `triggered_by`, оба лимита
считать не из чего.

**Файлы:**
- Изменить: `src/app/api/reels/route.ts`, `src/app/api/reels/[id]/sync/route.ts`
- Создать: `src/db/queries/sync-state.integration.test.ts` (первая часть)

**Интерфейсы:**
- Потребляет: колонки из задачи 2.
- Отдаёт наружу: ничего нового; гарантия, что новые строки `sync_runs`
  заполнены — на неё опираются задачи 4 и 5.

- [ ] **Шаг 1: Написать падающий интеграционный тест**

`src/db/queries/sync-state.integration.test.ts`. Ориентируйся на
`src/db/queries/list-reels.integration.test.ts` — он задаёт стиль: маркерные
адреса на `@example.invalid`, сид в `beforeAll`, уборка в `afterAll` каскадом
по `users.email`.

Тест ходит по HTTP на запущенный дев-сервер? **Нет.** Проверяем на уровне
базы: вставляем строку так же, как это делает эндпоинт, и убеждаемся, что
колонки заполнены. Проверка самих эндпоинтов — сквозным скриптом задачи 12.

```ts
import { eq } from 'drizzle-orm'
import { afterAll, beforeAll, describe, expect, it } from 'vitest'
import { db, reels, syncRuns, users } from '@/db'

const OWNER = 'sync-state-owner@example.invalid'

let userId: string
let reelId: string

beforeAll(async () => {
  await db.delete(users).where(eq(users.email, OWNER))

  const [user] = await db
    .insert(users)
    .values({ email: OWNER, passwordHash: 'x', displayName: 'Тест' })
    .returning({ id: users.id })
  userId = user.id

  const [reel] = await db
    .insert(reels)
    .values({
      userId,
      shortcode: 'SyncStateA',
      url: 'https://www.instagram.com/reel/SyncStateA/',
      syncStatus: 'ok',
    })
    .returning({ id: reels.id })
  reelId = reel.id
})

afterAll(async () => {
  await db.delete(users).where(eq(users.email, OWNER))
})

describe('sync_runs — новые колонки', () => {
  it('triggered_by по умолчанию manual', async () => {
    const [run] = await db
      .insert(syncRuns)
      .values({ reelId, userId, shortcode: 'SyncStateA', status: 'running' })
      .returning({ triggeredBy: syncRuns.triggeredBy })

    expect(run.triggeredBy).toBe('manual')
  })

  it('прогон переживает удаление рилса', async () => {
    const [reel] = await db
      .insert(reels)
      .values({
        userId,
        shortcode: 'SyncStateGone',
        url: 'https://www.instagram.com/reel/SyncStateGone/',
        syncStatus: 'ok',
      })
      .returning({ id: reels.id })

    const [run] = await db
      .insert(syncRuns)
      .values({
        reelId: reel.id,
        userId,
        shortcode: 'SyncStateGone',
        status: 'succeeded',
      })
      .returning({ id: syncRuns.id })

    await db.delete(reels).where(eq(reels.id, reel.id))

    // Раньше ON DELETE CASCADE стирал строку целиком — и вместе с ней
    // обнулялся любой счётчик, построенный на sync_runs.
    const [survivor] = await db.select().from(syncRuns).where(eq(syncRuns.id, run.id))

    expect(survivor).toBeDefined()
    expect(survivor.reelId).toBeNull()
    expect(survivor.userId).toBe(userId)
    expect(survivor.shortcode).toBe('SyncStateGone')
  })

  it('CHECK не пропускает выдуманный triggered_by', async () => {
    await expect(
      db
        .insert(syncRuns)
        .values({
          reelId,
          userId,
          shortcode: 'SyncStateA',
          status: 'running',
          // @ts-expect-error — проверяем защиту на уровне БД, а не типов
          triggeredBy: 'магия',
        }),
    ).rejects.toThrow()
  })
})
```

- [ ] **Шаг 2: Убедиться, что тест проходит**

```bash
npm run test:db -- sync-state
```

Он должен пройти **сразу**: задача 2 уже сделала всё нужное. Если падает —
миграция применилась не полностью, вернись к задаче 2.

- [ ] **Шаг 3: Заполнять поля в `POST /api/reels`**

В `src/app/api/reels/route.ts` найти вставку `syncRuns` и заменить:

```ts
    const [pendingRun] = await db
      .insert(syncRuns)
      .values({
        reelId: reel.id,
        userId: session.userId,
        shortcode: reel.shortcode,
        triggeredBy: 'manual',
        apifyRunId: null,
        status: 'running',
      })
      .returning({ id: syncRuns.id })
```

- [ ] **Шаг 4: Заполнять поля в `POST /api/reels/:id/sync`**

В `src/app/api/reels/[id]/sync/route.ts` та же правка:

```ts
    const [pendingRun] = await db
      .insert(syncRuns)
      .values({
        reelId: reel.id,
        userId: session.userId,
        shortcode: reel.shortcode,
        triggeredBy: 'manual',
        apifyRunId: null,
        status: 'running',
      })
      .returning({ id: syncRuns.id })
```

- [ ] **Шаг 5: Проверить руками на дев-сервере**

`curl` в Windows-консоли портит кириллицу — только клиентом на Node.
Положи скрипт в корень, прогони, удали.

```js
// check-fields.mjs
const BASE = 'http://localhost:3000'
const email = `fields-${Date.now()}@example.invalid`
const reg = await fetch(`${BASE}/api/auth/register`, {
  method: 'POST', headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ email, password: 'parolparol1', displayName: 'Поля' }),
})
const cookie = reg.headers.get('set-cookie').split(';')[0]
const added = await fetch(`${BASE}/api/reels`, {
  method: 'POST', headers: { 'content-type': 'application/json', cookie },
  body: JSON.stringify({ url: 'https://www.instagram.com/reel/DcXVbOOiyhL/' }),
})
console.log('добавление:', added.status)
console.log('аккаунт для уборки:', email)
```

Затем посмотреть в базе, что у свежей строки `sync_runs` заполнены `user_id`,
`shortcode` и `triggered_by = 'manual'`.

- [ ] **Шаг 6: Прогнать всё и закоммитить**

```bash
npm test && npm run test:db && npm run build && npm run lint
git add src/app/api/reels src/db/queries/sync-state.integration.test.ts
git commit -m "feat: пути ручной синхронизации пишут владельца, шорткод и источник"
```

---

### Задача 4: Троттлинг ключуется на паре и закрывает дыру с удалением

**Дыра, задокументированная в `AGENTS.md` с среза 5:** удалить рилс и вставить
ту же ссылку заново — и часовой троттлинг обойдён, потому что у нового рилса
новый `id`, а история старого стёрлась каскадом. Задача 2 сохранила историю;
осталось начать её читать.

**Файлы:**
- Изменить: `src/db/queries/sync-state.ts` (создать), `src/lib/sync/throttle.ts`
  (только комментарий), `src/app/api/reels/[id]/sync/route.ts`
- Тест: `src/db/queries/sync-state.integration.test.ts` (дописать)

**Интерфейсы:**
- Отдаёт наружу:
  ```ts
  /** Время последней попытки по стабильной паре. null — попыток не было. */
  async function lastAttemptFor(userId: string, shortcode: string): Promise<Date | null>
  ```
  Используется задачей 5 и маршрутом синхронизации.

- [ ] **Шаг 1: Написать падающий тест**

Дописать в `src/db/queries/sync-state.integration.test.ts`:

```ts
import { lastAttemptFor } from './sync-state'

describe('lastAttemptFor — троттлинг переживает удаление рилса', () => {
  it('без попыток отдаёт null', async () => {
    expect(await lastAttemptFor(userId, 'НикогдаНеБыло')).toBeNull()
  })

  it('видит попытку по паре пользователь + шорткод', async () => {
    const at = new Date(Date.now() - 10 * 60_000)
    await db.insert(syncRuns).values({
      reelId,
      userId,
      shortcode: 'SyncStateA',
      status: 'running',
      startedAt: at,
    })

    const found = await lastAttemptFor(userId, 'SyncStateA')

    expect(found).toBeInstanceOf(Date)
    expect(found!.getTime()).toBeGreaterThanOrEqual(at.getTime() - 1000)
  })

  it('ДЫРА ЗАКРЫТА: попытка видна после удаления и повторной вставки рилса', async () => {
    const code = 'SyncStateReadd'

    const [first] = await db
      .insert(reels)
      .values({ userId, shortcode: code, url: `https://x/${code}`, syncStatus: 'ok' })
      .returning({ id: reels.id })

    await db.insert(syncRuns).values({
      reelId: first.id,
      userId,
      shortcode: code,
      status: 'succeeded',
      startedAt: new Date(),
    })

    // Двухкликовая последовательность из ленты: удалить и вставить заново.
    await db.delete(reels).where(eq(reels.id, first.id))
    await db
      .insert(reels)
      .values({ userId, shortcode: code, url: `https://x/${code}`, syncStatus: 'ok' })

    // Рилс новый, id новый — но попытка та же и она обязана быть видна.
    expect(await lastAttemptFor(userId, code)).toBeInstanceOf(Date)
  })

  it('чужие попытки по тому же шорткоду не видны', async () => {
    const [stranger] = await db
      .insert(users)
      .values({
        email: 'sync-state-stranger@example.invalid',
        passwordHash: 'x',
        displayName: 'Чужой',
      })
      .returning({ id: users.id })

    await db.insert(syncRuns).values({
      userId: stranger.id,
      shortcode: 'ОбщийКод',
      status: 'succeeded',
      startedAt: new Date(),
    })

    expect(await lastAttemptFor(userId, 'ОбщийКод')).toBeNull()

    await db.delete(users).where(eq(users.id, stranger.id))
  })
})
```

Не забудь добавить `'sync-state-stranger@example.invalid'` в уборку `afterAll`.

- [ ] **Шаг 2: Убедиться, что тест падает**

```bash
npm run test:db -- sync-state
```

Ожидаем: FAIL, `Cannot find module './sync-state'`.

- [ ] **Шаг 3: Реализовать**

Создать `src/db/queries/sync-state.ts`:

```ts
import { and, desc, eq } from 'drizzle-orm'
import { db, syncRuns } from '@/db'

/**
 * Время последней попытки синхронизации по СТАБИЛЬНОЙ паре
 * «пользователь + шорткод».
 *
 * Почему не по `reel_id`. Пара `(user_id, shortcode)` — настоящая идентичность
 * копии рилса у блогера: в `reels` на ней стоит `uq_reels_user_shortcode`.
 * Она переживает удаление и повторную вставку, а `reel_id` — нет. Именно на
 * этом держалась дыра, задокументированная с среза 5: удалить рилс и вставить
 * ту же ссылку заново значило обойти часовой троттлинг и потратить лишний
 * кредит Apify. Каждый цикл — минус кредит.
 *
 * Считаются ПОПЫТКИ, а не успехи (см. `checkManualSync`): проваленный прогон
 * списывает тот же результат из месячного лимита, что и удачный.
 */
export async function lastAttemptFor(
  userId: string,
  shortcode: string,
): Promise<Date | null> {
  const [row] = await db
    .select({ startedAt: syncRuns.startedAt })
    .from(syncRuns)
    .where(and(eq(syncRuns.userId, userId), eq(syncRuns.shortcode, shortcode)))
    .orderBy(desc(syncRuns.startedAt))
    .limit(1)

  return row?.startedAt ?? null
}
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

```bash
npm run test:db -- sync-state
```

- [ ] **Шаг 5: Переключить маршрут синхронизации на пару**

В `src/app/api/reels/[id]/sync/route.ts` заменить блок чтения последней
попытки. Было — выборка по `reelId`; стало:

```ts
    // Ключ — ПАРА (user_id, shortcode), а не reel_id. Разница видна ровно
    // в одном сценарии, и он же был дырой: удалить рилс и вставить ту же
    // ссылку заново. Новый reel_id не помнил ничего, пара помнит.
    const lastAttempt = await lastAttemptFor(session.userId, reel.shortcode)

    const verdict = checkManualSync(lastAttempt, new Date())
    if (!verdict.allowed) return fail(verdict.message, 429)
```

Убрать ставший ненужным импорт `desc` и выборку `syncRuns`, если они больше
нигде в файле не используются. Добавить импорт:

```ts
import { lastAttemptFor } from '@/db/queries/sync-state'
```

- [ ] **Шаг 6: Применить тот же троттлинг в `POST /api/reels`**

**Без этого шага дыра не закрывается, и заголовок задачи врёт.**
`POST /api/reels` никогда не сверялся с часовым троттлингом. Изменить ключ
только в кнопке «Обновить» значит закрыть парадную дверь и оставить
распахнутой чёрную: сценарий «удалить → вставить ту же ссылку» идёт именно
через добавление.

В `src/app/api/reels/route.ts` добавить блок **после** дедупа и **до** резерва
бюджета:

```ts
    // Тот же часовой троттлинг, что у кнопки «Обновить», и по тому же ключу.
    // Здесь он и есть настоящее лекарство от дыры «удалил → вставил заново»:
    // рилс новый, id новый, но пара (user_id, shortcode) помнит попытку.
    //
    // Порядок важен. ПОСЛЕ дедупа: существующий рилс обязан по-прежнему
    // отвечать «уже добавлен», а не «слишком часто» — это разные ситуации
    // и разные подсказки. ДО резерва бюджета: за отклонённый запрос кредиты
    // не резервируем.
    const lastAttempt = await lastAttemptFor(session.userId, parsed.shortcode)
    const throttle = checkManualSync(lastAttempt, new Date())
    if (!throttle.allowed) return fail(throttle.message, 429)
```

Импорты:

```ts
import { lastAttemptFor } from '@/db/queries/sync-state'
import { checkManualSync } from '@/lib/sync/throttle'
```

> Добавление НОВОГО рилса это не ломает: у ссылки, которую блогер вставляет
> впервые, попыток в прошлом нет, `lastAttemptFor` отдаёт `null`, и
> `checkManualSync` пропускает.

- [ ] **Шаг 7: Обновить комментарий в `throttle.ts`**

В шапке `src/lib/sync/throttle.ts` дописать абзац:

```
 * Ключ троттлинга — ПАРА (user_id, shortcode), а не идентификатор рилса.
 * `reel_id` не переживает удаление, и на этом держалась дыра: «Удалить →
 * вставить ту же ссылку» давало новый рилс без единой попытки в прошлом,
 * и прогон стартовал немедленно. Источник времени — `lastAttemptFor`
 * в `src/db/queries/sync-state.ts`.
```

- [ ] **Шаг 8: Проверить дыру руками на дев-сервере**

Скрипт в корень, прогнать, удалить:

```js
// check-hole.mjs
const BASE = 'http://localhost:3000'
const email = `hole-${Date.now()}@example.invalid`
const URL_ = 'https://www.instagram.com/reel/DcXVbOOiyhL/'

const reg = await fetch(`${BASE}/api/auth/register`, {
  method: 'POST', headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ email, password: 'parolparol1', displayName: 'Дыра' }),
})
const cookie = reg.headers.get('set-cookie').split(';')[0]
const H = { 'content-type': 'application/json', cookie }

const first = await fetch(`${BASE}/api/reels`, { method: 'POST', headers: H, body: JSON.stringify({ url: URL_ }) })
const { id } = await first.json()
console.log('добавили:', first.status)

await fetch(`${BASE}/api/reels/${id}`, { method: 'DELETE', headers: H })
console.log('удалили')

const again = await fetch(`${BASE}/api/reels`, { method: 'POST', headers: H, body: JSON.stringify({ url: URL_ }) })
console.log('вставили заново:', again.status, '← ожидаем 429, а НЕ 202')
console.log(await again.text())
console.log('аккаунт для уборки:', email)
```

> Ожидаем **429** с текстом про час. Шаг 6 этой же задачи и делает так,
> чтобы он пришёл. Если пришёл 202 — шаг 6 не применён или блок встал не туда
> (например, до дедупа). Записать фактический результат.

- [ ] **Шаг 9: Проверка теста на зубы**

Вернуть в `lastAttemptFor` фильтр по `reelId` вместо пары → должен покраснеть
тест «ДЫРА ЗАКРЫТА». Записать фактический результат, вернуть обратно.

- [ ] **Шаг 10: Коммит**

```bash
npm test && npm run test:db && npm run build && npm run lint
git add src/db/queries/sync-state.ts src/db/queries/sync-state.integration.test.ts src/lib/sync/throttle.ts src/app/api/reels
git commit -m "fix: троттлинг на паре пользователь+шорткод, дыра с удалением закрыта"
```

---

### Задача 5: Лимит «N в минуту на пользователя»

**Файлы:**
- Создать: `src/lib/sync/rate-limit.ts`, `src/lib/sync/rate-limit.test.ts`
- Изменить: `src/db/queries/sync-state.ts`, `src/app/api/reels/route.ts`,
  `src/app/api/reels/[id]/sync/route.ts`
- Тест: `src/db/queries/sync-state.integration.test.ts` (дописать)

**Интерфейсы:**
- Отдаёт наружу:
  ```ts
  export const USER_SYNC_LIMIT = 5
  export const USER_SYNC_WINDOW_MS = 60_000
  function checkUserRate(recentAttempts: number, now: Date): ThrottleVerdict
  async function manualAttemptsSince(userId: string, since: Date): Promise<number>
  ```
  `ThrottleVerdict` уже объявлен в `src/lib/sync/throttle.ts` — импортировать
  оттуда, не объявлять второй.

- [ ] **Шаг 1: Написать падающие юнит-тесты**

`src/lib/sync/rate-limit.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { checkUserRate, USER_SYNC_LIMIT } from './rate-limit'

const NOW = new Date('2026-08-24T12:00:00Z')

describe('checkUserRate', () => {
  it('пропускает, когда попыток меньше лимита', () => {
    expect(checkUserRate(0, NOW)).toEqual({ allowed: true })
    expect(checkUserRate(USER_SYNC_LIMIT - 1, NOW)).toEqual({ allowed: true })
  })

  it('отказывает ровно на лимите', () => {
    // Пятая попытка уже записана — шестая не пройдёт.
    expect(checkUserRate(USER_SYNC_LIMIT, NOW).allowed).toBe(false)
  })

  it('отказывает и выше лимита', () => {
    expect(checkUserRate(USER_SYNC_LIMIT + 10, NOW).allowed).toBe(false)
  })

  it('в отказе человеческий текст без кода ошибки', () => {
    const verdict = checkUserRate(USER_SYNC_LIMIT, NOW)

    expect(verdict.allowed).toBe(false)
    if (verdict.allowed) return
    expect(verdict.message).toMatch(/[а-яё]/i)
    expect(verdict.message).not.toMatch(/\b\d{3}\b/)
  })

  it('отрицательное число не роняет и не блокирует', () => {
    // Счётчик из базы отрицательным быть не может, но падать на этом
    // сообщением про SQL — худший из возможных ответов.
    expect(checkUserRate(-1, NOW)).toEqual({ allowed: true })
  })
})
```

- [ ] **Шаг 2: Убедиться, что тесты падают**

```bash
npm test -- rate-limit
```

Ожидаем: FAIL, `Cannot find module './rate-limit'`.

- [ ] **Шаг 3: Реализовать чистую функцию**

`src/lib/sync/rate-limit.ts`:

```ts
import { plural } from '@/lib/format/plural'
import type { ThrottleVerdict } from './throttle'

/**
 * Лимит «не чаще N в минуту на пользователя», PLAN.md §7.
 *
 * Часовой троттлинг на рилс не спасает от разгона: рилсов у блогера много,
 * и обойти его можно, просто чередуя их. Этот лимит ограничивает человека
 * целиком, независимо от того, по какому рилсу он жмёт.
 *
 * Пять в минуту: человек, осмысленно добавляющий ссылки, в них укладывается;
 * залипший на кнопке — нет.
 */
export const USER_SYNC_LIMIT = 5
export const USER_SYNC_WINDOW_MS = 60_000

export function checkUserRate(recentAttempts: number, _now: Date): ThrottleVerdict {
  if (recentAttempts < USER_SYNC_LIMIT) return { allowed: true }

  return {
    allowed: false,
    message:
      `Слишком часто. Больше ${USER_SYNC_LIMIT} ` +
      `${plural(USER_SYNC_LIMIT, ['обновления', 'обновлений', 'обновлений'])} ` +
      'в минуту не получится — подожди немного и попробуй снова',
  }
}
```

> `_now` в сигнатуре не используется, но остаётся: она держит форму, общую
> с `checkManualSync`, и понадобится, если лимит станет зависеть от времени
> суток. Подчёркивание гасит правило линтера про неиспользуемый аргумент.

- [ ] **Шаг 4: Убедиться, что юнит-тесты проходят**

```bash
npm test -- rate-limit
```

- [ ] **Шаг 5: Написать падающий интеграционный тест на счётчик**

Дописать в `src/db/queries/sync-state.integration.test.ts`:

```ts
import { manualAttemptsSince } from './sync-state'

describe('manualAttemptsSince', () => {
  it('считает только свежие попытки', async () => {
    const [fresh] = await db
      .insert(users)
      .values({
        email: 'sync-state-counter@example.invalid',
        passwordHash: 'x',
        displayName: 'Счёт',
      })
      .returning({ id: users.id })

    const now = Date.now()
    for (const minutesAgo of [0, 0.5, 5]) {
      await db.insert(syncRuns).values({
        userId: fresh.id,
        shortcode: `C${minutesAgo}`,
        status: 'running',
        triggeredBy: 'manual',
        startedAt: new Date(now - minutesAgo * 60_000),
      })
    }

    // Окно в минуту захватывает две из трёх.
    expect(await manualAttemptsSince(fresh.id, new Date(now - 60_000))).toBe(2)

    await db.delete(users).where(eq(users.id, fresh.id))
  })

  it('НЕ считает прогоны крона', async () => {
    const [cronUser] = await db
      .insert(users)
      .values({
        email: 'sync-state-cron@example.invalid',
        passwordHash: 'x',
        displayName: 'Крон',
      })
      .returning({ id: users.id })

    // Тик крона тронул десять рилсов этого блогера. Он ничего не нажимал,
    // и его кнопка «Обновить» блокироваться не должна.
    for (let i = 0; i < 10; i++) {
      await db.insert(syncRuns).values({
        userId: cronUser.id,
        shortcode: `Cron${i}`,
        status: 'running',
        triggeredBy: 'cron',
        startedAt: new Date(),
      })
    }

    expect(await manualAttemptsSince(cronUser.id, new Date(Date.now() - 60_000))).toBe(0)

    await db.delete(users).where(eq(users.id, cronUser.id))
  })

  it('переживает удаление рилса', async () => {
    const [reel] = await db
      .insert(reels)
      .values({
        userId,
        shortcode: 'CounterSurvives',
        url: 'https://x/CounterSurvives',
        syncStatus: 'ok',
      })
      .returning({ id: reels.id })

    await db.insert(syncRuns).values({
      reelId: reel.id,
      userId,
      shortcode: 'CounterSurvives',
      status: 'running',
      triggeredBy: 'manual',
      startedAt: new Date(),
    })

    const before = await manualAttemptsSince(userId, new Date(Date.now() - 60_000))
    await db.delete(reels).where(eq(reels.id, reel.id))
    const after = await manualAttemptsSince(userId, new Date(Date.now() - 60_000))

    // Раньше CASCADE обнулял счётчик вместе с рилсом — то есть лимит
    // сбрасывался тем же действием, от которого должен защищать.
    expect(after).toBe(before)
  })
})
```

Добавить оба новых адреса в уборку `afterAll`.

- [ ] **Шаг 6: Реализовать счётчик**

Дописать в `src/db/queries/sync-state.ts`. Строку импорта из `drizzle-orm`
**дополнить**, а не заменить — `and`, `desc` и `eq` там уже есть с задачи 4:

```ts
// было:  import { and, desc, eq } from 'drizzle-orm'
// стало:
import { and, count, desc, eq, gte } from 'drizzle-orm'

/**
 * Сколько РУЧНЫХ попыток пользователь сделал начиная с момента `since`.
 *
 * `triggered_by = 'manual'` — не украшение. Без него тик крона, тронувший
 * десять рилсов блогера, засчитался бы как десять его попыток и заблокировал
 * бы ему кнопку при том, что он ничего не нажимал.
 *
 * Считается по `user_id`, а не через JOIN к `reels`: строка переживает
 * удаление рилса, и счётчик вместе с ней. Иначе лимит обнулялся бы тем же
 * действием, от которого защищает.
 */
export async function manualAttemptsSince(userId: string, since: Date): Promise<number> {
  const [row] = await db
    .select({ n: count() })
    .from(syncRuns)
    .where(
      and(
        eq(syncRuns.userId, userId),
        eq(syncRuns.triggeredBy, 'manual'),
        gte(syncRuns.startedAt, since),
      ),
    )

  return row?.n ?? 0
}
```

- [ ] **Шаг 7: Убедиться, что интеграционные проходят**

```bash
npm run test:db -- sync-state
```

- [ ] **Шаг 8: Повесить лимит на ОБА пути**

В `src/app/api/reels/[id]/sync/route.ts` — сразу после `requireSession()`
и получения рилса, **до** проверки часового троттлинга:

```ts
import { checkUserRate, USER_SYNC_WINDOW_MS } from '@/lib/sync/rate-limit'
import { manualAttemptsSince } from '@/db/queries/sync-state'

    // Лимит на человека идёт ПЕРВЫМ: он дешевле часового (один COUNT против
    // выборки с сортировкой) и отсекает разгон по разным рилсам, который
    // часовой троттлинг пропускает по построению.
    const attempts = await manualAttemptsSince(
      session.userId,
      new Date(Date.now() - USER_SYNC_WINDOW_MS),
    )
    const rate = checkUserRate(attempts, new Date())
    if (!rate.allowed) return fail(rate.message, 429)
```

В `src/app/api/reels/route.ts` — тот же блок сразу после `requireSession()`,
**до** резерва бюджета и вставки рилса:

```ts
    // Добавление рилса ТОЖЕ запускает прогон и тоже стоит кредита.
    // Ограничить только кнопку «Обновить» значило бы оставить открытой
    // вторую дверь — ровно ту, через которую утекает бюджет в сценарии
    // «удалил → вставил заново».
    const attempts = await manualAttemptsSince(
      session.userId,
      new Date(Date.now() - USER_SYNC_WINDOW_MS),
    )
    const rate = checkUserRate(attempts, new Date())
    if (!rate.allowed) return fail(rate.message, 429)
```

- [ ] **Шаг 9: Проверить руками**

Скрипт в корень, прогнать, удалить:

```js
// check-rate.mjs
const BASE = 'http://localhost:3000'
const email = `rate-${Date.now()}@example.invalid`
const reg = await fetch(`${BASE}/api/auth/register`, {
  method: 'POST', headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ email, password: 'parolparol1', displayName: 'Лимит' }),
})
const cookie = reg.headers.get('set-cookie').split(';')[0]
const H = { 'content-type': 'application/json', cookie }

const codes = ['DcXVbOOiyhL', 'DcJsGN3tYER', 'DcYUN9wIWdD', 'AaAaAaAaAaA', 'BbBbBbBbBbB', 'CcCcCcCcCcC', 'DdDdDdDdDdD']
for (const [i, code] of codes.entries()) {
  const r = await fetch(`${BASE}/api/reels`, {
    method: 'POST', headers: H,
    body: JSON.stringify({ url: `https://www.instagram.com/reel/${code}/` }),
  })
  const body = await r.json().catch(() => ({}))
  console.log(`${i + 1}. ${code} → ${r.status} ${body.error ?? ''}`)
}
console.log('\nожидаем: первые 5 по 202, дальше 429 с русским текстом')
console.log('аккаунт для уборки:', email)
```

- [ ] **Шаг 10: Проверка тестов на зубы**

Убрать `eq(syncRuns.triggeredBy, 'manual')` из `manualAttemptsSince` → должен
покраснеть тест «НЕ считает прогоны крона». Записать фактический результат,
вернуть.

- [ ] **Шаг 11: Коммит**

```bash
npm test && npm run test:db && npm run build && npm run lint
git add src/lib/sync/rate-limit.ts src/lib/sync/rate-limit.test.ts src/db/queries/sync-state.ts src/db/queries/sync-state.integration.test.ts src/app/api/reels
git commit -m "feat: лимит N обновлений в минуту на пользователя"
```

---

### Задача 6: Выборка «кому пора»

**Файлы:**
- Изменить: `src/db/queries/sync-state.ts`
- Тест: `src/db/queries/sync-state.integration.test.ts` (дописать)

**Интерфейсы:**
- Отдаёт наружу:
  ```ts
  type DueReel = { id: string; url: string; shortcode: string; userId: string }
  async function dueReels(limit: number, now?: Date): Promise<DueReel[]>
  ```
  Используется задачей 8.

- [ ] **Шаг 1: Написать падающий тест**

Дописать:

```ts
import { dueReels } from './sync-state'

describe('dueReels — кого крон берёт в работу', () => {
  const DUE_OWNER = 'due-owner@example.invalid'
  let dueUserId: string

  beforeAll(async () => {
    await db.delete(users).where(eq(users.email, DUE_OWNER))
    const [u] = await db
      .insert(users)
      .values({ email: DUE_OWNER, passwordHash: 'x', displayName: 'Пора' })
      .returning({ id: users.id })
    dueUserId = u.id

    const mk = (code: string, nextSyncAt: Date | null, syncStatus: 'ok' | 'unavailable') =>
      db.insert(reels).values({
        userId: dueUserId,
        shortcode: code,
        url: `https://www.instagram.com/reel/${code}/`,
        syncStatus,
        nextSyncAt,
      })

    const ago = (m: number) => new Date(Date.now() - m * 60_000)
    const ahead = (m: number) => new Date(Date.now() + m * 60_000)

    await mk('DueOld', ago(120), 'ok') // просрочен сильнее всех
    await mk('DueNow', ago(5), 'ok') // просрочен недавно
    await mk('DueLater', ahead(60), 'ok') // ещё рано
    await mk('DueGone', ago(120), 'unavailable') // снят с расписания статусом
    await mk('DueNull', null, 'ok') // снят с расписания отсутствием даты
  })

  afterAll(async () => {
    await db.delete(users).where(eq(users.email, DUE_OWNER))
  })

  it('берёт только просроченных', async () => {
    const codes = (await dueReels(50)).map((r) => r.shortcode)

    expect(codes).toContain('DueOld')
    expect(codes).toContain('DueNow')
    expect(codes).not.toContain('DueLater')
  })

  it('пропускает недоступные и снятые с расписания', async () => {
    const codes = (await dueReels(50)).map((r) => r.shortcode)

    // ingestReel снимает недоступный рилс с расписания, обнуляя next_sync_at.
    // Опрашивать приватную или удалённую запись вечно — прямой расход бюджета
    // без единого шанса что-то получить.
    expect(codes).not.toContain('DueGone')
    expect(codes).not.toContain('DueNull')
  })

  it('самый просроченный идёт первым', async () => {
    const mine = (await dueReels(50)).filter((r) => r.userId === dueUserId)

    expect(mine[0].shortcode).toBe('DueOld')
  })

  it('уважает лимит батча', async () => {
    expect((await dueReels(1)).length).toBe(1)
  })

  it('отдаёт всё, что нужно для запуска прогона', async () => {
    const [first] = await dueReels(1)

    expect(typeof first.id).toBe('string')
    expect(typeof first.url).toBe('string')
    expect(typeof first.shortcode).toBe('string')
    expect(typeof first.userId).toBe('string')
  })
})
```

- [ ] **Шаг 2: Убедиться, что тест падает**

```bash
npm run test:db -- sync-state
```

- [ ] **Шаг 3: Реализовать**

Дописать в `src/db/queries/sync-state.ts`:

```ts
import { asc, isNotNull, lte, ne } from 'drizzle-orm'
import { reels } from '@/db'

export type DueReel = { id: string; url: string; shortcode: string; userId: string }

/**
 * Рилсы, которым пора обновиться.
 *
 * Опирается на частичный индекс `idx_reels_due`, объявленный ещё в срезе 1
 * ровно под этот запрос: `ON (next_sync_at) WHERE sync_status <> 'unavailable'`.
 *
 * Два способа сняться с расписания, и оба намеренные:
 *  — `sync_status = 'unavailable'` — приватная или удалённая запись;
 *  — `next_sync_at IS NULL` — то же самое, проставляет `ingestReel`.
 * Опрашивать такие рилсы вечно значит тратить бюджет без шанса что-то
 * получить.
 *
 * Порядок — от самого просроченного: если очередь всё-таки набежала, первым
 * обновится тот, кто дольше всех ждёт, а не случайный.
 */
export async function dueReels(limit: number, now = new Date()): Promise<DueReel[]> {
  return db
    .select({
      id: reels.id,
      url: reels.url,
      shortcode: reels.shortcode,
      userId: reels.userId,
    })
    .from(reels)
    .where(
      and(
        isNotNull(reels.nextSyncAt),
        lte(reels.nextSyncAt, now),
        ne(reels.syncStatus, 'unavailable'),
      ),
    )
    .orderBy(asc(reels.nextSyncAt))
    .limit(limit)
}
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

```bash
npm run test:db -- sync-state
```

- [ ] **Шаг 5: Проверка тестов на зубы**

Убрать `ne(reels.syncStatus, 'unavailable')` → должен покраснеть тест про
недоступные. Убрать `orderBy` → должен покраснеть тест про порядок. Записать
фактические результаты, вернуть.

- [ ] **Шаг 6: Коммит**

```bash
npm run test:db && npm run build && npm run lint
git add src/db/queries/sync-state.ts src/db/queries/sync-state.integration.test.ts
git commit -m "feat: выборка рилсов, которым пора обновиться"
```

---

### Задача 7: Фаза 1 — сбор результатов завершённых прогонов

**Файлы:**
- Создать: `src/lib/sync/run-cron.ts`
- Тест: `src/lib/sync/run-cron.integration.test.ts`

**Интерфейсы:**
- Потребляет: `getRun`, `getDatasetItems` из `@/lib/apify/client`,
  `ingestReel` из `@/db/queries/ingest`.
- Отдаёт наружу:
  ```ts
  export const STUCK_RUN_MS = 6 * 3_600_000
  async function collectFinishedRuns(now?: Date): Promise<number>
  ```
  Возвращает число рилсов, по которым данные приняты. Используется задачей 9.

- [ ] **Шаг 1: Написать падающий тест**

`src/lib/sync/run-cron.integration.test.ts`:

```ts
import { eq } from 'drizzle-orm'
import { afterAll, beforeAll, describe, expect, it } from 'vitest'
import { db, reels, syncRuns, users } from '@/db'
import { collectFinishedRuns, STUCK_RUN_MS } from './run-cron'

/**
 * Фаза 1 крона на ЖИВОЙ базе и МОКЕ Apify.
 *
 * `APIFY_MOCK=1` в `.env.local` означает, что `getRun` всегда отвечает
 * SUCCEEDED, а `getDatasetItems` отдаёт фикстуры. Шорткоды в тесте берутся
 * из `fixtures/apify/` — иначе `ingestReel` не найдёт совпадения и запишет
 * рилсу `unavailable`.
 */

const OWNER = 'cron-collect@example.invalid'
const FIXTURE_CODE = 'DcXVbOOiyhL'

let userId: string

beforeAll(async () => {
  await db.delete(users).where(eq(users.email, OWNER))
  const [u] = await db
    .insert(users)
    .values({ email: OWNER, passwordHash: 'x', displayName: 'Сбор' })
    .returning({ id: users.id })
  userId = u.id
})

afterAll(async () => {
  await db.delete(users).where(eq(users.email, OWNER))
})

async function seedRunningReel(code: string, startedAt = new Date()) {
  const [reel] = await db
    .insert(reels)
    .values({
      userId,
      shortcode: code,
      url: `https://www.instagram.com/reel/${code}/`,
      syncStatus: 'pending',
    })
    .returning({ id: reels.id })

  // Мок кодирует запрошенные шорткоды прямо в идентификатор прогона:
  // см. shortcodesFrom в src/lib/apify/client.ts.
  const [run] = await db
    .insert(syncRuns)
    .values({
      reelId: reel.id,
      userId,
      shortcode: code,
      triggeredBy: 'cron',
      apifyRunId: `mock:${code}`,
      status: 'running',
      startedAt,
    })
    .returning({ id: syncRuns.id })

  return { reelId: reel.id, runId: run.id }
}

describe('collectFinishedRuns', () => {
  it('забирает данные и переводит рилс в ok', async () => {
    const { reelId, runId } = await seedRunningReel(FIXTURE_CODE)

    const collected = await collectFinishedRuns()
    expect(collected).toBeGreaterThanOrEqual(1)

    const [reel] = await db.select().from(reels).where(eq(reels.id, reelId))
    expect(reel.syncStatus).toBe('ok')
    expect(reel.lastSyncedAt).toBeInstanceOf(Date)

    const [run] = await db.select().from(syncRuns).where(eq(syncRuns.id, runId))
    expect(run.status).toBe('succeeded')
    expect(run.finishedAt).toBeInstanceOf(Date)
  })

  it('ставит next_sync_at, чтобы рилс вернулся в расписание', async () => {
    const { reelId } = await seedRunningReel(FIXTURE_CODE)
    await collectFinishedRuns()

    const [reel] = await db.select().from(reels).where(eq(reels.id, reelId))
    expect(reel.nextSyncAt).toBeInstanceOf(Date)
  })

  it('зависший прогон помечает failed, а не тянет вечно', async () => {
    const stale = new Date(Date.now() - STUCK_RUN_MS - 60_000)
    const { runId } = await seedRunningReel('StuckCode', stale)

    await collectFinishedRuns()

    const [run] = await db.select().from(syncRuns).where(eq(syncRuns.id, runId))
    expect(run.status).toBe('failed')
    expect(run.error).toMatch(/[а-яё]/i)
  })

  it('прогон без apify_run_id не трогает: запуск ещё не состоялся', async () => {
    const [reel] = await db
      .insert(reels)
      .values({
        userId,
        shortcode: 'NoRunId',
        url: 'https://x/NoRunId',
        syncStatus: 'pending',
      })
      .returning({ id: reels.id })

    const [run] = await db
      .insert(syncRuns)
      .values({
        reelId: reel.id,
        userId,
        shortcode: 'NoRunId',
        triggeredBy: 'cron',
        apifyRunId: null,
        status: 'running',
      })
      .returning({ id: syncRuns.id })

    await collectFinishedRuns()

    const [after] = await db.select().from(syncRuns).where(eq(syncRuns.id, run.id))
    expect(after.status).toBe('running')
  })

  it('прогон удалённого рилса закрывается без падения', async () => {
    const { reelId, runId } = await seedRunningReel(FIXTURE_CODE)
    await db.delete(reels).where(eq(reels.id, reelId))

    // reel_id стал NULL — принимать данные некуда, но и падать нельзя.
    await expect(collectFinishedRuns()).resolves.toBeGreaterThanOrEqual(0)

    const [run] = await db.select().from(syncRuns).where(eq(syncRuns.id, runId))
    expect(run.status).toBe('succeeded')
  })
})
```

- [ ] **Шаг 2: Убедиться, что тест падает**

```bash
npm run test:db -- run-cron
```

- [ ] **Шаг 3: Реализовать**

`src/lib/sync/run-cron.ts`:

```ts
import { and, eq, inArray, isNotNull } from 'drizzle-orm'
import { db, reels, syncRuns } from '@/db'
import { ingestReel } from '@/db/queries/ingest'
import { getDatasetItems, getRun } from '@/lib/apify/client'

/**
 * Фоновая синхронизация. Две фазы одного тика, обе ничего не ждут.
 *
 * Ждать завершения прогона внутри одного вызова нельзя: Netlify обрывает
 * функцию через 10 секунд, а прогон Apify идёт 14–19 (замер среза 3).
 * Поэтому тик забирает результаты прогонов, запущенных ПРОШЛЫМ тиком,
 * и стартует новые для следующего.
 */

/**
 * Прогон, висящий дольше этого, считается мёртвым.
 *
 * Шесть часов с запасом больше и самого прогона (19 с), и самой частой
 * ступени расписания (1 ч): живой прогон под этот порог не попадёт никогда.
 * Без порога зависший прогон блокирует свои рилсы навсегда — они остаются
 * в `pending`, а `next_sync_at` у них в будущем.
 */
export const STUCK_RUN_MS = 6 * 3_600_000

/**
 * Фаза 1: забрать результаты завершённых прогонов.
 *
 * Строки группируются по `apify_run_id`, потому что один прогон покрывает
 * весь батч: спрашивать статус и тянуть датасет по разу на каждый рилс
 * значило бы сделать десять одинаковых запросов вместо одного.
 *
 * Возвращает число рилсов, по которым данные приняты.
 */
export async function collectFinishedRuns(now = new Date()): Promise<number> {
  const running = await db
    .select({
      id: syncRuns.id,
      reelId: syncRuns.reelId,
      apifyRunId: syncRuns.apifyRunId,
      startedAt: syncRuns.startedAt,
    })
    .from(syncRuns)
    .where(and(eq(syncRuns.status, 'running'), isNotNull(syncRuns.apifyRunId)))

  const byRun = new Map<string, typeof running>()
  for (const row of running) {
    const key = row.apifyRunId!
    const bucket = byRun.get(key)
    if (bucket) bucket.push(row)
    else byRun.set(key, [row])
  }

  let collected = 0

  for (const [apifyRunId, rows] of byRun) {
    const ids = rows.map((r) => r.id)
    const oldest = Math.min(...rows.map((r) => r.startedAt.getTime()))

    if (now.getTime() - oldest > STUCK_RUN_MS) {
      await failRuns(ids, 'Прогон не завершился за отведённое время', now)
      continue
    }

    let handle
    try {
      handle = await getRun(apifyRunId)
    } catch {
      // Apify недоступен — не трогаем ничего, следующий тик повторит.
      continue
    }

    if (handle.status === 'RUNNING') continue

    if (handle.status === 'FAILED' || !handle.datasetId) {
      await failRuns(ids, 'Прогон Apify завершился ошибкой', now)
      continue
    }

    const items = await getDatasetItems(handle.datasetId)

    for (const row of rows) {
      // reel_id стал NULL — рилс удалили, пока прогон шёл. Принимать данные
      // некуда, но прогон всё равно надо закрыть.
      if (!row.reelId) continue

      try {
        await ingestReel(row.reelId, items, now)
        collected++
      } catch {
        // Один битый рилс не должен ронять весь батч: остальные девять
        // уже оплачены и их данные лежат в этом же датасете.
      }
    }

    await db
      .update(syncRuns)
      .set({ status: 'succeeded', finishedAt: now })
      .where(inArray(syncRuns.id, ids))
  }

  return collected
}

async function failRuns(ids: number[], error: string, now: Date): Promise<void> {
  await db
    .update(syncRuns)
    .set({ status: 'failed', error, finishedAt: now })
    .where(inArray(syncRuns.id, ids))

  // Рилсы этих прогонов остаются в pending без надежды продвинуться.
  // Переводим в failed, чтобы их забрал существующий путь «Повторить».
  const rows = await db
    .select({ reelId: syncRuns.reelId })
    .from(syncRuns)
    .where(inArray(syncRuns.id, ids))

  const reelIds = rows.map((r) => r.reelId).filter((id): id is string => id !== null)
  if (reelIds.length === 0) return

  await db
    .update(reels)
    .set({
      syncStatus: 'failed',
      syncError: 'Не получилось забрать данные. Попробуй обновить рилс ещё раз',
    })
    .where(inArray(reels.id, reelIds))
}
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

```bash
npm run test:db -- run-cron
```

- [ ] **Шаг 5: Проверка тестов на зубы**

Убрать проверку `STUCK_RUN_MS` → должен покраснеть тест про зависший прогон.
Убрать `if (!row.reelId) continue` → должен покраснеть тест про удалённый
рилс. Записать фактические результаты, вернуть.

- [ ] **Шаг 6: Коммит**

```bash
npm run test:db && npm run build && npm run lint
git add src/lib/sync/run-cron.ts src/lib/sync/run-cron.integration.test.ts
git commit -m "feat: фаза сбора — крон забирает результаты завершённых прогонов"
```

---

### Задача 8: Фаза 2 — запуск батча

**Файлы:**
- Изменить: `src/lib/sync/run-cron.ts`
- Тест: `src/lib/sync/run-cron.integration.test.ts` (дописать)

**Интерфейсы:**
- Потребляет: `dueReels` (задача 6), `tryReserve`, `startRun`, `isMock`.
- Отдаёт наружу:
  ```ts
  export const CRON_BATCH_SIZE = 10
  type TickReport = { collected: number; started: number; skipped: 'budget' | null }
  async function runCronTick(now?: Date, batchSize?: number): Promise<TickReport>
  ```
  Используется задачей 9.

- [ ] **Шаг 1: Написать падающий тест**

Дописать в `src/lib/sync/run-cron.integration.test.ts`:

```ts
import { runCronTick, CRON_BATCH_SIZE } from './run-cron'

describe('runCronTick — фаза запуска', () => {
  async function seedDue(code: string, minutesOverdue = 30) {
    const [reel] = await db
      .insert(reels)
      .values({
        userId,
        shortcode: code,
        url: `https://www.instagram.com/reel/${code}/`,
        syncStatus: 'ok',
        nextSyncAt: new Date(Date.now() - minutesOverdue * 60_000),
      })
      .returning({ id: reels.id })
    return reel.id
  }

  it('запускает прогон и пишет строку sync_runs на каждый рилс', async () => {
    const reelId = await seedDue('TickOne')

    const report = await runCronTick()
    expect(report.started).toBeGreaterThanOrEqual(1)

    const runs = await db.select().from(syncRuns).where(eq(syncRuns.reelId, reelId))
    expect(runs).toHaveLength(1)
    expect(runs[0].triggeredBy).toBe('cron')
    expect(runs[0].userId).toBe(userId)
    expect(runs[0].shortcode).toBe('TickOne')
  })

  it('весь батч уходит ОДНИМ прогоном Apify', async () => {
    await seedDue('TickBatchA')
    await seedDue('TickBatchB')

    await runCronTick()

    const runs = await db
      .select({ apifyRunId: syncRuns.apifyRunId, shortcode: syncRuns.shortcode })
      .from(syncRuns)
      .where(eq(syncRuns.userId, userId))

    const batch = runs.filter((r) => r.shortcode?.startsWith('TickBatch'))
    expect(batch).toHaveLength(2)
    // Один прогон на оба рилса: платим за результаты, а не за прогоны,
    // и десять запросов к Apify вместо одного — трата времени функции.
    expect(new Set(batch.map((r) => r.apifyRunId)).size).toBe(1)
  })

  it('сдвигает next_sync_at, чтобы следующий тик не взял тот же рилс', async () => {
    const reelId = await seedDue('TickShift')

    await runCronTick()

    const [reel] = await db.select().from(reels).where(eq(reels.id, reelId))
    expect(reel.nextSyncAt!.getTime()).toBeGreaterThan(Date.now())

    // Второй тик подряд не должен запустить этот рилс повторно.
    const before = await db.select().from(syncRuns).where(eq(syncRuns.reelId, reelId))
    await runCronTick()
    const after = await db.select().from(syncRuns).where(eq(syncRuns.reelId, reelId))
    expect(after.length).toBe(before.length)
  })

  it('уважает размер батча', async () => {
    for (let i = 0; i < 4; i++) await seedDue(`TickLimit${i}`)

    const report = await runCronTick(new Date(), 2)

    expect(report.started).toBeLessThanOrEqual(2)
  })

  it('когда обновлять некого, тик проходит вхолостую без ошибок', async () => {
    // Разгребаем всё, что осталось от прошлых тестов.
    for (let i = 0; i < 5; i++) await runCronTick()

    const report = await runCronTick()

    expect(report.started).toBe(0)
    expect(report.skipped).toBeNull()
  })
})
```

- [ ] **Шаг 2: Убедиться, что тест падает**

```bash
npm run test:db -- run-cron
```

- [ ] **Шаг 3: Реализовать**

Дописать в `src/lib/sync/run-cron.ts`:

```ts
import { tryReserve } from '@/db/queries/budget'
import { dueReels } from '@/db/queries/sync-state'
import { isMock, startRun } from '@/lib/apify/client'

/**
 * Сколько рилсов берём за тик.
 *
 * Фаза 2 — один вызов Apify плюс N вставок; фаза 1 — один `getRun`, один
 * `getDatasetItems` и до N приёмов со снапшотами. Приёмы и есть дорогая
 * часть. Десять взяты как заведомо безопасное для десяти секунд Netlify
 * и ЗАМЕРЕНЫ в задаче 9 — не менять на глаз.
 *
 * При 96 тиках в сутки это потолок в 960 рилсов, то есть очередь «кому пора»
 * физически не может накопиться на демо-наборе в 60 рилсов.
 */
export const CRON_BATCH_SIZE = 10

/**
 * Провизорный сдвиг расписания в момент СТАРТА.
 *
 * Настоящее значение проставит `ingestReel` при успешном приёме. Этот сдвиг
 * нужен только чтобы следующий тик не увидел тот же рилс всё ещё просроченным
 * и не оплатил его второй раз. Час выбран как заведомо больше периода крона
 * (15 минут); если прогон провалится и приём не состоится, рилс честно
 * вернётся в очередь через час.
 */
const PROVISIONAL_SHIFT_MS = 3_600_000

export type TickReport = {
  collected: number
  started: number
  /** Почему фаза 2 не пошла. `null` — пошла нормально. */
  skipped: 'budget' | null
}

export async function runCronTick(
  now = new Date(),
  batchSize = CRON_BATCH_SIZE,
): Promise<TickReport> {
  // Порядок фиксирован: СНАЧАЛА собрать, потом запустить. Обратный заставил
  // бы фазу 1 разбирать прогоны, стартовавшие секунду назад и заведомо
  // не готовые.
  const collected = await collectFinishedRuns(now)

  const due = await dueReels(batchSize, now)
  if (due.length === 0) return { collected, started: 0, skipped: null }

  // Бюджет резервируется ДО запуска. Обратный порядок означает, что при
  // исчерпанном лимите кредиты уже потрачены, а узнаём мы после.
  // В режиме мока счётчик не трогаем: из тарифа Apify не расходуется ничего.
  if (!isMock() && !(await tryReserve(due.length))) {
    return { collected, started: 0, skipped: 'budget' }
  }

  // Строки пишутся ДО startRun — тот же порядок и та же причина, что в обоих
  // ручных путях: если запуск бросит исключение, рилсы не должны остаться
  // осиротевшими в pending без единой строки в sync_runs.
  const inserted = await db
    .insert(syncRuns)
    .values(
      due.map((reel) => ({
        reelId: reel.id,
        userId: reel.userId,
        shortcode: reel.shortcode,
        triggeredBy: 'cron' as const,
        apifyRunId: null,
        status: 'running' as const,
        startedAt: now,
      })),
    )
    .returning({ id: syncRuns.id })

  const runIds = inserted.map((r) => r.id)

  let run
  try {
    run = await startRun(due.map((reel) => reel.url))
  } catch {
    await failRuns(runIds, 'Не получилось запустить прогон Apify', now)
    return { collected, started: 0, skipped: null }
  }

  await db
    .update(syncRuns)
    .set({
      apifyRunId: run.runId,
      status: run.status === 'FAILED' ? 'failed' : 'running',
    })
    .where(inArray(syncRuns.id, runIds))

  await db
    .update(reels)
    .set({
      syncStatus: 'pending',
      syncError: null,
      nextSyncAt: new Date(now.getTime() + PROVISIONAL_SHIFT_MS),
    })
    .where(
      inArray(
        reels.id,
        due.map((reel) => reel.id),
      ),
    )

  return { collected, started: due.length, skipped: null }
}
```

> Если задача 1 показала, что `resultsLimit` ограничивает **весь прогон**,
> здесь же поправь `startRun` в `src/lib/apify/client.ts`: он должен слать
> `resultsLimit: urls.length`, а не единицу. Ровно длину массива, не больше —
> иначе ссылка на профиль вместо рилса снова сможет грести ленту.

- [ ] **Шаг 4: Убедиться, что тесты проходят**

```bash
npm run test:db -- run-cron
```

- [ ] **Шаг 5: Проверка тестов на зубы**

Убрать сдвиг `nextSyncAt` → должен покраснеть тест «следующий тик не берёт тот
же рилс». Заменить один `startRun` на цикл по рилсам → должен покраснеть тест
«весь батч уходит одним прогоном». Записать фактические результаты, вернуть.

- [ ] **Шаг 6: Коммит**

```bash
npm run test:db && npm run build && npm run lint
git add src/lib/sync/run-cron.ts src/lib/sync/run-cron.integration.test.ts
git commit -m "feat: фаза запуска — крон отправляет батч одним прогоном Apify"
```

---

### Задача 9: Эндпоинт `/api/cron/sync` и замер времени тика

**Файлы:**
- Создать: `src/app/api/cron/sync/route.ts`

**Интерфейсы:**
- Потребляет: `runCronTick` (задача 8).
- Отдаёт наружу: `POST /api/cron/sync` → `200 { collected, started, skipped, ms }`,
  `401` без верного секрета. Используется задачами 10 и 12.

- [ ] **Шаг 1: Реализовать эндпоинт**

`src/app/api/cron/sync/route.ts`:

```ts
import { fail, handleError, ok } from '@/lib/api/respond'
import { runCronTick } from '@/lib/sync/run-cron'

export const runtime = 'nodejs'

/**
 * Точка входа фоновой синхронизации.
 *
 * Тонкий слой: разобрать секрет, позвать тик, вернуть отчёт. Вся логика —
 * в `src/lib/sync/run-cron.ts`, потому что её надо тестировать напрямую,
 * без поднятия HTTP.
 *
 * Секрет читается из ЗАГОЛОВКА, а не из query-параметра: строка запроса
 * оседает в логах прокси, браузера и самого планировщика, а заголовок — нет.
 *
 * Маршрут лежит под `/api/`, то есть вне матчера `proxy.ts` (`/app/:path*`),
 * поэтому сессия здесь не проверяется и редиректа на `/login` не будет.
 */
export async function POST(request: Request) {
  try {
    const secret = process.env.CRON_SECRET

    // Пустой секрет в окружении — это не «пускать всех», а поломка настройки.
    if (!secret) return fail('Синхронизация не настроена', 503)

    const header = request.headers.get('authorization') ?? ''
    if (header !== `Bearer ${secret}`) return fail('Нужен ключ', 401)

    const startedAt = Date.now()
    const report = await runCronTick()
    const ms = Date.now() - startedAt

    // Время тика в ответе — не украшение: у функции Netlify десять секунд,
    // и единственный способ заметить приближение к потолку — смотреть на
    // это число в логах планировщика.
    console.log('[cron]', JSON.stringify({ ...report, ms }))

    return ok({ ...report, ms })
  } catch (error) {
    return handleError(error)
  }
}
```

- [ ] **Шаг 2: Проверить защиту**

Дев-сервер запущен. Скрипт в корень, прогнать, удалить:

```js
// check-cron.mjs
const BASE = 'http://localhost:3000'
const secret = process.env.CRON_SECRET

const noKey = await fetch(`${BASE}/api/cron/sync`, { method: 'POST' })
console.log('без ключа        :', noKey.status, '← ожидаем 401')

const badKey = await fetch(`${BASE}/api/cron/sync`, {
  method: 'POST', headers: { authorization: 'Bearer nonsense' },
})
console.log('с чужим ключом   :', badKey.status, '← ожидаем 401')

const good = await fetch(`${BASE}/api/cron/sync`, {
  method: 'POST', headers: { authorization: `Bearer ${secret}` },
})
console.log('с верным ключом  :', good.status, await good.text())
```

Запуск: `node --env-file=.env.local check-cron.mjs`

- [ ] **Шаг 3: ЗАМЕРИТЬ время тика на полном батче**

Это и есть проверка числа `CRON_BATCH_SIZE = 10`, обещанная в спеке.

Насей десять просроченных рилсов на тестовом аккаунте и прогони тик, читая
`ms` из ответа. Скрипт в корень, прогнать, удалить:

```js
// measure-tick.mjs
import postgres from 'postgres'
const sql = postgres(process.env.DATABASE_URL, { prepare: false })
const BASE = 'http://localhost:3000'

const email = `tick-${Date.now()}@example.invalid`
const reg = await fetch(`${BASE}/api/auth/register`, {
  method: 'POST', headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ email, password: 'parolparol1', displayName: 'Замер' }),
})
console.log('аккаунт:', email, reg.status)

const [{ id: userId }] = await sql`SELECT id FROM users WHERE email = ${email}`
const past = new Date(Date.now() - 3600_000)
for (let i = 0; i < 10; i++) {
  await sql`
    INSERT INTO reels (user_id, shortcode, url, sync_status, next_sync_at)
    VALUES (${userId}, ${'Tick' + i}, ${'https://www.instagram.com/reel/DcXVbOOiyhL/'}, 'ok', ${past})
  `
}

const one = await fetch(`${BASE}/api/cron/sync`, {
  method: 'POST', headers: { authorization: `Bearer ${process.env.CRON_SECRET}` },
})
console.log('тик 1 (запуск батча):', await one.text())

const two = await fetch(`${BASE}/api/cron/sync`, {
  method: 'POST', headers: { authorization: `Bearer ${process.env.CRON_SECRET}` },
})
console.log('тик 2 (сбор батча)  :', await two.text())

await sql.end()
console.log('аккаунт для уборки:', email)
```

**Записать оба числа `ms` в отчёт.** Критерий:

| Замер | Вывод |
|---|---|
| Оба тика < 5000 мс | запас двукратный, `CRON_BATCH_SIZE = 10` оставить |
| 5000–8000 мс | запас тонкий, снизить до 5 и перезамерить |
| > 8000 мс | не влезаем, снизить до 3 и перезамерить |

Помни: замер идёт на **моке**, где Apify отвечает мгновенно. Живой `getRun`
и `getDatasetItems` добавят по круговой задержке каждый. Поэтому порог
и стоит на 5000, а не на 9000.

- [ ] **Шаг 4: Убрать за собой**

```bash
rm -f check-cron.mjs measure-tick.mjs
```

- [ ] **Шаг 5: Коммит**

```bash
npm test && npm run test:db && npm run build && npm run lint
git add src/app/api/cron/sync/route.ts src/lib/sync/run-cron.ts
git commit -m "feat: эндпоинт фоновой синхронизации с проверкой ключа"
```

---

### Задача 10: Планируемая функция Netlify

**Файлы:**
- Создать: `netlify/functions/sync-cron.mts`
- Изменить: `netlify.toml`

- [ ] **Шаг 1: Написать функцию-триггер**

`netlify/functions/sync-cron.mts`:

```ts
import type { Config } from '@netlify/functions'

/**
 * Планировщик фоновой синхронизации.
 *
 * ТОНКИЙ ТРИГГЕР И НИЧЕГО БОЛЬШЕ. Netlify даёт планируемой функции 30 секунд
 * против десяти у обычной, и соблазн перенести сюда логику есть. Нельзя:
 * этот файл лежит ВНЕ сборки Next.js и не импортирует ни `ingestReel`,
 * ни `computeNextSyncAt`, ни `tryReserve`. Продублировать их значит завести
 * вторую реализацию приёма данных, которая разойдётся с первой на первой же
 * правке.
 *
 * Поэтому функция делает один запрос на собственный `/api/cron/sync`,
 * где живёт вся логика и куда ходят тесты.
 */
export default async function handler(): Promise<Response> {
  const base = process.env.NEXT_PUBLIC_APP_URL
  const secret = process.env.CRON_SECRET

  if (!base || !secret) {
    console.error('[cron] нет NEXT_PUBLIC_APP_URL или CRON_SECRET')
    return new Response('not configured', { status: 500 })
  }

  const response = await fetch(`${base}/api/cron/sync`, {
    method: 'POST',
    headers: { authorization: `Bearer ${secret}` },
  })

  const body = await response.text()
  console.log('[cron]', response.status, body)

  return new Response(body, { status: response.status })
}

export const config: Config = {
  // Каждые 15 минут. Самая частая ступень расписания — час, так что четыре
  // тика в час дают запас на пропущенный и не создают лишней нагрузки.
  schedule: '*/15 * * * *',
}
```

- [ ] **Шаг 2: Поставить типы Netlify**

```bash
npm install --save-dev @netlify/functions
```

- [ ] **Шаг 3: Убедиться, что сборка Next.js не подхватила функцию**

```bash
npm run build
```

В списке маршрутов не должно появиться ничего нового: `netlify/functions/`
лежит вне `src/` и Next.js его не видит.

- [ ] **Шаг 4: Проверить, что расписание задано**

Расписание объявлено экспортом `config` в самом файле, поэтому в
`netlify.toml` дублировать его **не нужно**. Добавить только комментарий,
чтобы следующий читатель не искал:

```toml
# Планируемая функция синхронизации объявляет своё расписание сама —
# экспортом `config.schedule` в netlify/functions/sync-cron.mts.
# Дублировать его здесь не нужно: два источника правды разъедутся.
```

- [ ] **Шаг 5: Записать в отчёт, что проверить после деплоя**

Локально планируемая функция не запускается — расписание исполняет Netlify.
После пуша в `main` проверить в панели Netlify: Functions → `sync-cron`
должна появиться с пометкой Scheduled и следующим запуском.

- [ ] **Шаг 6: Коммит**

```bash
npm run build && npm run lint
git add netlify/functions/sync-cron.mts netlify.toml package.json package-lock.json
git commit -m "feat: планируемая функция Netlify запускает синхронизацию раз в 15 минут"
```

---

### Задача 11: Лог различает «вручную» и «по расписанию»

**Файлы:**
- Изменить: `src/db/queries/reel-detail.ts`, `src/components/reels/sync-log.tsx`
- Тест: `src/db/queries/reel-detail.integration.test.ts` (дописать)

- [ ] **Шаг 1: Написать падающий тест**

Дописать в `src/db/queries/reel-detail.integration.test.ts` внутрь блока
`describe('reelDetail — лог синхронизаций', ...)`:

```ts
  it('различает ручной прогон и прогон по расписанию', async () => {
    await db.insert(syncRuns).values({
      reelId: ids.mine,
      userId: ids.owner,
      shortcode: 'DetailMine',
      triggeredBy: 'cron',
      status: 'succeeded',
      apifyRunId: 'mock:ccc',
      startedAt: ago(2),
      finishedAt: ago(2),
    })

    const detail = await reelDetail(ids.owner, ids.mine)

    expect(detail!.runs.some((r) => r.triggeredBy === 'cron')).toBe(true)
    expect(detail!.runs.some((r) => r.triggeredBy === 'manual')).toBe(true)
  })
```

- [ ] **Шаг 2: Убедиться, что тест падает**

```bash
npm run test:db -- reel-detail
```

- [ ] **Шаг 3: Отдать поле наружу**

В `src/db/queries/reel-detail.ts` дописать в тип `DetailRun`:

```ts
  /** Кто запустил: блогер кнопкой или расписание. */
  triggeredBy: 'manual' | 'cron'
```

и в выборку прогонов:

```ts
        triggeredBy: syncRuns.triggeredBy,
```

Поле уже попадёт в результат через существующий spread — проверить, что
деструктуризация `({ apifyRunId, ...run })` его не отбрасывает.

- [ ] **Шаг 4: Показать в интерфейсе**

В `src/components/reels/sync-log.tsx` рядом с меткой «фикстуры» добавить:

```tsx
            {run.triggeredBy === 'cron' && (
              <span
                className="rounded-full bg-black/5 px-2 py-0.5 text-[11px] text-[var(--muted)]"
                title="Запущено расписанием, а не вручную"
              >
                по расписанию
              </span>
            )}
```

- [ ] **Шаг 5: Убедиться, что тесты проходят**

```bash
npm run test:db -- reel-detail
```

- [ ] **Шаг 6: Посмотреть глазами**

Открыть экран рилса, у которого есть прогон крона (после замера задачи 9 такие
есть). Убедиться, что пометка видна и не ломает вёрстку на 375px.

- [ ] **Шаг 7: Коммит**

```bash
npm test && npm run test:db && npm run build && npm run lint
git add src/db/queries/reel-detail.ts src/db/queries/reel-detail.integration.test.ts src/components/reels/sync-log.tsx
git commit -m "feat: лог синхронизаций отличает расписание от ручного обновления"
```

---

### Задача 12: Сквозная проверка, живой прогон и документы

**Файлы:**
- Создать: `scripts/e2e-cron.mjs`
- Изменить: `package.json`, `AGENTS.md`, `PLAN.md`, этот план

- [ ] **Шаг 1: Написать сквозной скрипт**

`scripts/e2e-cron.mjs` по образцу `scripts/e2e-dashboard.mjs`: своя сессия
с куками, `check()` со счётчиком, ненулевой выход при провале, чтение ответов
клиентом на Node — `curl` в Windows-консоли портит кириллицу.

Скрипту нужен прямой доступ к базе: сдвинуть `next_sync_at` в прошлое через
API нельзя. Таблицы импортировать **прямо из `src/db/schema`** — `export *`
не виден из скриптов, это записанные грабли.

```js
import { eq } from 'drizzle-orm'
import { db } from '../src/db/index.ts'
import { reels, syncRuns, users } from '../src/db/schema.ts'

const BASE = process.env.E2E_BASE ?? 'http://localhost:3000'
const SECRET = process.env.CRON_SECRET
const CODE = 'DcXVbOOiyhL'
const URL_ = `https://www.instagram.com/reel/${CODE}/`

let failed = 0
const check = (label, ok, detail = '') => {
  console.log(`  ${ok ? '✅' : '❌'} ${label}${detail ? ': ' + detail : ''}`)
  if (!ok) failed++
}

const tick = async (auth = `Bearer ${SECRET}`) => {
  const r = await fetch(`${BASE}/api/cron/sync`, {
    method: 'POST',
    headers: auth ? { authorization: auth } : {},
  })
  const json = await r.json().catch(() => ({}))
  return { status: r.status, ...json }
}

console.log('
── Ключ')
check('без заголовка — 401', (await tick(null)).status === 401)
check('с чужим ключом — 401', (await tick('Bearer nonsense')).status === 401)
check('с верным ключом — 200', (await tick()).status === 200)

console.log('
── Полный цикл на фикстурах')
const email = `cron-${Date.now()}@example.invalid`
const reg = await fetch(`${BASE}/api/auth/register`, {
  method: 'POST',
  headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ email, password: 'parolparol1', displayName: 'Крон' }),
})
const cookie = reg.headers.get('set-cookie').split(';')[0]
const H = { 'content-type': 'application/json', cookie }

const added = await fetch(`${BASE}/api/reels`, {
  method: 'POST', headers: H, body: JSON.stringify({ url: URL_ }),
})
const { id: reelId } = await added.json()
check('рилс добавлен', added.status === 202, String(added.status))

// Дожидаемся, пока добавление доедет, и насильно просрочиваем рилс.
await new Promise((r) => setTimeout(r, 3000))
await db
  .update(reels)
  .set({ nextSyncAt: new Date(Date.now() - 3_600_000), syncStatus: 'ok' })
  .where(eq(reels.id, reelId))

const one = await tick()
check('тик 1 запустил батч', one.started >= 1, JSON.stringify(one))

const two = await tick()
check('тик 2 забрал данные', two.collected >= 1, JSON.stringify(two))

const [after] = await db.select().from(reels).where(eq(reels.id, reelId))
check('рилс вышел в ok', after.syncStatus === 'ok', after.syncStatus)
check('расписание сдвинуто в будущее', after.nextSyncAt > new Date())

const three = await tick()
check('тик 3 не берёт тот же рилс', three.started === 0, JSON.stringify(three))

console.log('
── Прогон крона помечен как крон')
const runs = await db.select().from(syncRuns).where(eq(syncRuns.reelId, reelId))
check('есть строка с triggered_by = cron', runs.some((r) => r.triggeredBy === 'cron'))
check('есть строка с triggered_by = manual', runs.some((r) => r.triggeredBy === 'manual'))

console.log('
── Время тика укладывается в бюджет функции')
for (const [label, t] of [['тик 1', one], ['тик 2', two], ['тик 3', three]]) {
  check(`${label} быстрее 8 с`, typeof t.ms === 'number' && t.ms < 8000, `${t.ms} мс`)
}

console.log('
── Лимит на пользователя')
const email2 = `cron-rate-${Date.now()}@example.invalid`
const reg2 = await fetch(`${BASE}/api/auth/register`, {
  method: 'POST',
  headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ email: email2, password: 'parolparol1', displayName: 'Лимит' }),
})
const cookie2 = reg2.headers.get('set-cookie').split(';')[0]
const H2 = { 'content-type': 'application/json', cookie: cookie2 }

const codes = ['AaAaAaAaAaA', 'BbBbBbBbBbB', 'CcCcCcCcCcC', 'DdDdDdDdDdD', 'EeEeEeEeEeE', 'FfFfFfFfFfF']
const statuses = []
for (const code of codes) {
  const r = await fetch(`${BASE}/api/reels`, {
    method: 'POST', headers: H2,
    body: JSON.stringify({ url: `https://www.instagram.com/reel/${code}/` }),
  })
  statuses.push({ status: r.status, body: await r.json().catch(() => ({})) })
}
check('пятое добавление ещё проходит', statuses[4].status === 202, String(statuses[4].status))
check('шестое — 429', statuses[5].status === 429, String(statuses[5].status))
check('в отказе русский текст без кода',
  /[а-яё]/i.test(statuses[5].body.error ?? '') && !/\d{3}/.test(statuses[5].body.error ?? ''),
  statuses[5].body.error)

console.log('
── Дыра «удалил → вставил заново» закрыта')
// Ждём, пока минутное окно лимита истечёт, иначе поймаем не тот отказ.
await new Promise((r) => setTimeout(r, 61_000))
await fetch(`${BASE}/api/reels/${reelId}`, { method: 'DELETE', headers: H })
const readd = await fetch(`${BASE}/api/reels`, {
  method: 'POST', headers: H, body: JSON.stringify({ url: URL_ }),
})
const readdBody = await readd.json().catch(() => ({}))
check('повторная вставка троттлится', readd.status === 429,
  `${readd.status} ${readdBody.error ?? ''}`)

console.log('
── Тестовые аккаунты этого прогона (удалить одним запросом)')
for (const e of [email, email2]) console.log(`  ${e}`)

console.log(failed ? `
${failed} проверок упало` : '
Все проверки прошли')
process.exit(failed ? 1 : 0)
```

> Проверка дыры ждёт минуту намеренно. Без паузы повторная вставка упрётся
> в лимит на пользователя, а не в часовой троттлинг, и тест окажется
> зелёным по неверной причине — классическая ловушка «тест проходит, но
> проверяет не то».
>
> Хелпер `session()` из `e2e-dashboard.mjs` здесь не нужен — достаточно двух
> явных кук.

- [ ] **Шаг 2: Скрипт в `package.json`**

```json
"e2e:cron": "node --env-file=.env.local scripts/e2e-cron.mjs",
```

> `--env-file` здесь нужен, в отличие от остальных `e2e:*`: скрипту нужны
> `CRON_SECRET` и `DATABASE_URL`.

- [ ] **Шаг 3: Прогнать всё**

```bash
npm test
npm run test:db
npm run build
npm run lint
```

Плюс при запущенном дев-сервере:

```bash
npm run e2e:feed
npm run e2e:dashboard
npm run e2e:cron
```

Записать фактические числа — они пойдут в `AGENTS.md`.

- [ ] **Шаг 4: ЖИВОЙ прогон крона**

Единственный живой вызов Apify в срезе, кроме разведки задачи 1.

1. Завести тестовый аккаунт на `@example.invalid`, добавить **один** рилс.
2. Временно убрать `APIFY_MOCK=1` из `.env.local`, перезапустить дев-сервер.
3. Сдвинуть `next_sync_at` рилса в прошлое.
4. Прогнать тик, дождаться минуту, прогнать второй тик.
5. Убедиться: в `sync_runs.apify_run_id` **настоящий** идентификатор, а не
   `mock:`; у рилса появился снапшот; `apify_usage.results` вырос ровно на 1.
6. Вернуть `APIFY_MOCK=1`.

Записать в отчёт: сколько кредитов потрачено, сколько осталось (`usage()`).

- [ ] **Шаг 5: Исправить `PLAN.md`**

- §7: снять пометку «пока не реализован» с лимита на пользователя, дописать,
  что дыра «удалил → вставил заново» закрыта ключом `(user_id, shortcode)`.
- §15: срез 7 → ✅, **и заменить «cron-job.org» на Netlify Scheduled
  Functions** — сервис в спеке был назван без обоснования и проверку
  не пережил.

- [ ] **Шаг 6: Обновить `AGENTS.md`**

- срез 7 → ✅, следующий — 8;
- фактические числа тестов;
- «Что уже есть у бэкенда» — крон и лимиты;
- **в грабли:** планируемая функция Netlify получает 30 секунд против 10
  у обычной, но логику в неё переносить нельзя — она вне сборки Next.js;
- **в грабли:** `ON DELETE CASCADE` на `sync_runs.reel_id` обнулял любой
  счётчик, построенный на этой таблице; лимиты, которые считают попытки,
  обязаны переживать удаление того, к чему привязаны;
- **закрыть хвосты:** лимит «N в минуту на пользователя» и обход часового
  троттлинга через удаление — оба сделаны;
- **в хвосты, если задача 1 дала это:** поведение `resultsLimit` при батче.

- [ ] **Шаг 7: Дописать в этот план секции «Статус», «Отклонения от плана»,
      «Проверка тестов на зубы»**

По образцу [`2026-08-24-dashboard.md`](2026-08-24-dashboard.md).

- [ ] **Шаг 8: Уборка базы**

Собрать адреса всех тестовых аккаунтов среза и **перечислить в отчёте**.
Не удалять самостоятельно: база общая с продом, удаление необратимо,
решение за человеком.

- [ ] **Шаг 9: Коммит**

```bash
git add scripts/e2e-cron.mjs package.json AGENTS.md PLAN.md docs/superpowers/plans/2026-08-24-sync.md
git commit -m "docs: закрыть срез 7, сквозная проверка синхронизации"
```

---

## Критерий готовности среза

- [ ] `npm test` зелёный, тестов больше 278
- [ ] `npm run test:db` зелёный, тестов больше 96
- [ ] `npm run build` и `npm run lint` без ошибок
- [ ] `npm run e2e:feed`, `npm run e2e:dashboard`, `npm run e2e:cron` — всё прошло
- [ ] Рилс проходит цикл `pending → ok` **без единого действия пользователя**,
      за два тика крона
- [ ] Батч уходит **одним** прогоном Apify — проверено тестом
- [ ] Шестое добавление за минуту даёт 429 с русским текстом
- [ ] «Удалить → вставить ту же ссылку» даёт 429, а не запускает прогон
- [ ] Прогон крона **не** блокирует пользователю кнопку «Обновить»
- [ ] Время тика на батче из десяти замерено и записано
- [ ] Живой прогон подтверждён: настоящий `apify_run_id`, счётчик вырос на 1
- [ ] Планируемая функция видна в панели Netlify как Scheduled

---

## Риски, о которых стоит знать заранее

| Риск | Что делать |
|---|---|
| `resultsLimit: 1` ограничивает весь прогон | Узнаём в задаче 1. Тогда `resultsLimit: urls.length` в задаче 8 — ровно длина массива, не больше |
| Актор не принимает батч вовсе | **BLOCKED в задаче 1.** Заменить на прогон-на-рилс: тот же расход кредитов, но N вызовов и N строк — задачи 7 и 8 переписываются |
| Тик не влезает в 10 секунд | Замер в задаче 9 со ступенчатым снижением батча: 10 → 5 → 3 |
| Миграция на живой базе прода пошла не так | Изменения аддитивные: три новые колонки и смена правила удаления. Откат — вернуть `ON DELETE CASCADE`; данные при этом не теряются, потому что колонки только добавлялись |
| Планируемая функция не запускается на Netlify | Проверяется только после деплоя. Запасной вариант — GitHub Actions cron по тому же эндпоинту: он опаздывает, но работает |
| Крон разбудил Neon и съел вычислительные часы | 96 тиков в сутки по секунде — около часа CU в месяц из ста. Замер `ms` в задаче 9 покажет фактическое |
